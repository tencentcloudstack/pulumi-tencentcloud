// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import { input as inputs, output as outputs } from "../types";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a antiddos packet filter config
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@pulumi/tencentcloud";
 *
 * const packetFilterConfig = new tencentcloud.Antiddos.PacketFilterConfig("packet_filter_config", {
 *     instanceId: "bgp-00000ry7",
 *     packetFilterConfig: {
 *         action: "drop",
 *         depth: 1,
 *         dportEnd: 80,
 *         dportStart: 80,
 *         isNot: 0,
 *         matchBegin: "begin_l5",
 *         matchType: "pcre",
 *         offset: 1,
 *         pktlenMax: 1400,
 *         pktlenMin: 1400,
 *         protocol: "all",
 *         sportEnd: 8080,
 *         sportStart: 8080,
 *         str: "a",
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * antiddos packet_filter_config can be imported using the id, e.g.
 *
 * ```sh
 *  $ pulumi import tencentcloud:Antiddos/packetFilterConfig:PacketFilterConfig packet_filter_config packet_filter_config_id
 * ```
 */
export class PacketFilterConfig extends pulumi.CustomResource {
    /**
     * Get an existing PacketFilterConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: PacketFilterConfigState, opts?: pulumi.CustomResourceOptions): PacketFilterConfig {
        return new PacketFilterConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Antiddos/packetFilterConfig:PacketFilterConfig';

    /**
     * Returns true if the given object is an instance of PacketFilterConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is PacketFilterConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === PacketFilterConfig.__pulumiType;
    }

    /**
     * resource id.
     */
    public readonly instanceId!: pulumi.Output<string>;
    /**
     * Feature filtering configuration.
     */
    public readonly packetFilterConfig!: pulumi.Output<outputs.Antiddos.PacketFilterConfigPacketFilterConfig>;

    /**
     * Create a PacketFilterConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: PacketFilterConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: PacketFilterConfigArgs | PacketFilterConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as PacketFilterConfigState | undefined;
            resourceInputs["instanceId"] = state ? state.instanceId : undefined;
            resourceInputs["packetFilterConfig"] = state ? state.packetFilterConfig : undefined;
        } else {
            const args = argsOrState as PacketFilterConfigArgs | undefined;
            if ((!args || args.instanceId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            if ((!args || args.packetFilterConfig === undefined) && !opts.urn) {
                throw new Error("Missing required property 'packetFilterConfig'");
            }
            resourceInputs["instanceId"] = args ? args.instanceId : undefined;
            resourceInputs["packetFilterConfig"] = args ? args.packetFilterConfig : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(PacketFilterConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering PacketFilterConfig resources.
 */
export interface PacketFilterConfigState {
    /**
     * resource id.
     */
    instanceId?: pulumi.Input<string>;
    /**
     * Feature filtering configuration.
     */
    packetFilterConfig?: pulumi.Input<inputs.Antiddos.PacketFilterConfigPacketFilterConfig>;
}

/**
 * The set of arguments for constructing a PacketFilterConfig resource.
 */
export interface PacketFilterConfigArgs {
    /**
     * resource id.
     */
    instanceId: pulumi.Input<string>;
    /**
     * Feature filtering configuration.
     */
    packetFilterConfig: pulumi.Input<inputs.Antiddos.PacketFilterConfigPacketFilterConfig>;
}
