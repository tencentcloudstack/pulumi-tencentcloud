// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a SSM secret version using the terraform-plugin-framework implementation.
 *
 * This is the framework-based counterpart of `tencentcloud.Ssm.SecretVersion`. It additionally supports a write-only attribute `secretStringWo` whose value is never persisted in pulumi preview or state.
 *
 * > **Note:** Exactly one of `secretBinary`, `secretString` or `secretStringWo` must be set. A maximum of 10 versions can exist under one credential. Only credentials in the enabled or disabled state can have new versions added.
 *
 * ## Example Usage
 *
 * ### Text-type credential, plaintext
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.ssm.Secret("example", {
 *     secretName: "tf-example",
 *     description: "desc.",
 *     recoveryWindowInDays: 0,
 *     isEnabled: true,
 * });
 * const v1 = new tencentcloud.ssm.SecretVersionV2("v1", {
 *     secretName: example.secretName,
 *     versionId: "v1",
 *     secretString: "this is secret string",
 * });
 * ```
 *
 * ### Write-only credential (value not persisted in state)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const wo = new tencentcloud.ssm.SecretVersionV2("wo", {
 *     secretName: example.secretName,
 *     versionId: "wo",
 *     secretStringWo: "this value is never persisted in plan or state",
 * });
 * ```
 *
 * ### Binary credential, encoded using base64
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const v2 = new tencentcloud.ssm.SecretVersionV2("v2", {
 *     secretName: example.secretName,
 *     versionId: "v2",
 *     secretBinary: "MTIzMTIzMTIzMTIzMTIzQQ==",
 * });
 * ```
 *
 * ## Import
 *
 * SSM secret version (v2) can be imported using `secretName#versionId`, e.g.
 * ```sh
 * $ pulumi import tencentcloud:Ssm/secretVersionV2:SecretVersionV2 v1 test#v1
 * ```
 */
export class SecretVersionV2 extends pulumi.CustomResource {
    /**
     * Get an existing SecretVersionV2 resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecretVersionV2State, opts?: pulumi.CustomResourceOptions): SecretVersionV2 {
        return new SecretVersionV2(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Ssm/secretVersionV2:SecretVersionV2';

    /**
     * Returns true if the given object is an instance of SecretVersionV2.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecretVersionV2 {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecretVersionV2.__pulumiType;
    }

    /**
     * Binary credential information, encoded using Base64. Conflicts with secretString and secret_string_wo.
     */
    declare public readonly secretBinary: pulumi.Output<string | undefined>;
    /**
     * Specifies the name of the credential to which the new version is to be added.
     */
    declare public readonly secretName: pulumi.Output<string>;
    /**
     * Text-based credential information in plaintext (Base64 encoding is not required). Conflicts with secretBinary and secret_string_wo.
     */
    declare public readonly secretString: pulumi.Output<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Write-only text-based credential information. Its value is sent to the API but never persisted in pulumi preview or state. Conflicts with secretBinary and secret_string.
     */
    declare public readonly secretStringWo: pulumi.Output<string | undefined>;
    /**
     * Specifies the version ID for the newly added version. It can be up to 64 bytes in length and must consist of a combination of letters, numbers, and the characters `-`, `_`, or `.`, starting with a letter or a number.
     */
    declare public readonly versionId: pulumi.Output<string>;

    /**
     * Create a SecretVersionV2 resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecretVersionV2Args, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecretVersionV2Args | SecretVersionV2State, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecretVersionV2State | undefined;
            resourceInputs["secretBinary"] = state?.secretBinary;
            resourceInputs["secretName"] = state?.secretName;
            resourceInputs["secretString"] = state?.secretString;
            resourceInputs["secretStringWo"] = state?.secretStringWo;
            resourceInputs["versionId"] = state?.versionId;
        } else {
            const args = argsOrState as SecretVersionV2Args | undefined;
            if (args?.secretName === undefined && !opts.urn) {
                throw new Error("Missing required property 'secretName'");
            }
            if (args?.versionId === undefined && !opts.urn) {
                throw new Error("Missing required property 'versionId'");
            }
            resourceInputs["secretBinary"] = args?.secretBinary ? pulumi.secret(args.secretBinary) : undefined;
            resourceInputs["secretName"] = args?.secretName;
            resourceInputs["secretString"] = args?.secretString ? pulumi.secret(args.secretString) : undefined;
            resourceInputs["secretStringWo"] = args?.secretStringWo ? pulumi.secret(args.secretStringWo) : undefined;
            resourceInputs["versionId"] = args?.versionId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["secretBinary", "secretString", "secretStringWo"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(SecretVersionV2.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecretVersionV2 resources.
 */
export interface SecretVersionV2State {
    /**
     * Binary credential information, encoded using Base64. Conflicts with secretString and secret_string_wo.
     */
    secretBinary?: pulumi.Input<string | undefined>;
    /**
     * Specifies the name of the credential to which the new version is to be added.
     */
    secretName?: pulumi.Input<string | undefined>;
    /**
     * Text-based credential information in plaintext (Base64 encoding is not required). Conflicts with secretBinary and secret_string_wo.
     */
    secretString?: pulumi.Input<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Write-only text-based credential information. Its value is sent to the API but never persisted in pulumi preview or state. Conflicts with secretBinary and secret_string.
     */
    secretStringWo?: pulumi.Input<string | undefined>;
    /**
     * Specifies the version ID for the newly added version. It can be up to 64 bytes in length and must consist of a combination of letters, numbers, and the characters `-`, `_`, or `.`, starting with a letter or a number.
     */
    versionId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SecretVersionV2 resource.
 */
export interface SecretVersionV2Args {
    /**
     * Binary credential information, encoded using Base64. Conflicts with secretString and secret_string_wo.
     */
    secretBinary?: pulumi.Input<string | undefined>;
    /**
     * Specifies the name of the credential to which the new version is to be added.
     */
    secretName: pulumi.Input<string>;
    /**
     * Text-based credential information in plaintext (Base64 encoding is not required). Conflicts with secretBinary and secret_string_wo.
     */
    secretString?: pulumi.Input<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Write-only text-based credential information. Its value is sent to the API but never persisted in pulumi preview or state. Conflicts with secretBinary and secret_string.
     */
    secretStringWo?: pulumi.Input<string | undefined>;
    /**
     * Specifies the version ID for the newly added version. It can be up to 64 bytes in length and must consist of a combination of letters, numbers, and the characters `-`, `_`, or `.`, starting with a letter or a number.
     */
    versionId: pulumi.Input<string>;
}
