// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to restore object
 *
 * > **NOTE:** The current resource does not support cdc.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const objectRestore = new tencentcloud.cos.ObjectRestoreOperation("object_restore", {
 *     bucket: "keep-test-1308919341",
 *     key: "test-restore.txt",
 *     tier: "Expedited",
 *     days: 2,
 * });
 * ```
 */
export class ObjectRestoreOperation extends pulumi.CustomResource {
    /**
     * Get an existing ObjectRestoreOperation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ObjectRestoreOperationState, opts?: pulumi.CustomResourceOptions): ObjectRestoreOperation {
        return new ObjectRestoreOperation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Cos/objectRestoreOperation:ObjectRestoreOperation';

    /**
     * Returns true if the given object is an instance of ObjectRestoreOperation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ObjectRestoreOperation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ObjectRestoreOperation.__pulumiType;
    }

    /**
     * Bucket.
     */
    declare public readonly bucket: pulumi.Output<string>;
    /**
     * Specifies the valid duration of the restored temporary copy in days.
     */
    declare public readonly days: pulumi.Output<number>;
    /**
     * Object key.
     */
    declare public readonly key: pulumi.Output<string>;
    /**
     * when restoring, Tier can be specified as the supported recovery model.
     * There are three recovery models for recovering archived storage type data, which are:
     * - Expedited: quick retrieval mode, and the recovery task can be completed in 1-5 minutes.
     * - Standard: standard retrieval mode. Recovery task is completed within 3-5 hours.
     * - Bulk: batch retrieval mode, and the recovery task is completed within 5-12 hours.
     * For deep recovery archive storage type data, there are two recovery models, which are:
     * - Standard: standard retrieval mode, recovery time is 12-24 hours.
     * - Bulk: batch retrieval mode, recovery time is 24-48 hours.
     */
    declare public readonly tier: pulumi.Output<string>;

    /**
     * Create a ObjectRestoreOperation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ObjectRestoreOperationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ObjectRestoreOperationArgs | ObjectRestoreOperationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ObjectRestoreOperationState | undefined;
            resourceInputs["bucket"] = state?.bucket;
            resourceInputs["days"] = state?.days;
            resourceInputs["key"] = state?.key;
            resourceInputs["tier"] = state?.tier;
        } else {
            const args = argsOrState as ObjectRestoreOperationArgs | undefined;
            if (args?.bucket === undefined && !opts.urn) {
                throw new Error("Missing required property 'bucket'");
            }
            if (args?.days === undefined && !opts.urn) {
                throw new Error("Missing required property 'days'");
            }
            if (args?.key === undefined && !opts.urn) {
                throw new Error("Missing required property 'key'");
            }
            if (args?.tier === undefined && !opts.urn) {
                throw new Error("Missing required property 'tier'");
            }
            resourceInputs["bucket"] = args?.bucket;
            resourceInputs["days"] = args?.days;
            resourceInputs["key"] = args?.key;
            resourceInputs["tier"] = args?.tier;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ObjectRestoreOperation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ObjectRestoreOperation resources.
 */
export interface ObjectRestoreOperationState {
    /**
     * Bucket.
     */
    bucket?: pulumi.Input<string>;
    /**
     * Specifies the valid duration of the restored temporary copy in days.
     */
    days?: pulumi.Input<number>;
    /**
     * Object key.
     */
    key?: pulumi.Input<string>;
    /**
     * when restoring, Tier can be specified as the supported recovery model.
     * There are three recovery models for recovering archived storage type data, which are:
     * - Expedited: quick retrieval mode, and the recovery task can be completed in 1-5 minutes.
     * - Standard: standard retrieval mode. Recovery task is completed within 3-5 hours.
     * - Bulk: batch retrieval mode, and the recovery task is completed within 5-12 hours.
     * For deep recovery archive storage type data, there are two recovery models, which are:
     * - Standard: standard retrieval mode, recovery time is 12-24 hours.
     * - Bulk: batch retrieval mode, recovery time is 24-48 hours.
     */
    tier?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a ObjectRestoreOperation resource.
 */
export interface ObjectRestoreOperationArgs {
    /**
     * Bucket.
     */
    bucket: pulumi.Input<string>;
    /**
     * Specifies the valid duration of the restored temporary copy in days.
     */
    days: pulumi.Input<number>;
    /**
     * Object key.
     */
    key: pulumi.Input<string>;
    /**
     * when restoring, Tier can be specified as the supported recovery model.
     * There are three recovery models for recovering archived storage type data, which are:
     * - Expedited: quick retrieval mode, and the recovery task can be completed in 1-5 minutes.
     * - Standard: standard retrieval mode. Recovery task is completed within 3-5 hours.
     * - Bulk: batch retrieval mode, and the recovery task is completed within 5-12 hours.
     * For deep recovery archive storage type data, there are two recovery models, which are:
     * - Standard: standard retrieval mode, recovery time is 12-24 hours.
     * - Bulk: batch retrieval mode, recovery time is 24-48 hours.
     */
    tier: pulumi.Input<string>;
}
