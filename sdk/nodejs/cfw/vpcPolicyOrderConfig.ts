// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a CFW vpc policy order config
 *
 * > **NOTE:** If resource `tencentcloud.Cfw.VpcPolicyOrderConfig` is used to sort resource `tencentcloud.Cfw.VpcPolicy`, all instances of resource `tencentcloud.Cfw.VpcPolicy` must be configured simultaneously, and the sorting of this resource cannot be declared elsewhere.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example1 = new tencentcloud.cfw.VpcPolicy("example1", {
 *     sourceContent: "1.1.1.1/0",
 *     sourceType: "net",
 *     destContent: "192.168.0.1",
 *     destType: "net",
 *     protocol: "ANY",
 *     ruleAction: "log",
 *     port: "-1/-1",
 *     description: "description.",
 *     enable: "true",
 *     fwGroupId: "ALL",
 * });
 * const example2 = new tencentcloud.cfw.VpcPolicy("example2", {
 *     sourceContent: "2.2.2.2/0",
 *     sourceType: "net",
 *     destContent: "192.168.0.2",
 *     destType: "net",
 *     protocol: "ANY",
 *     ruleAction: "log",
 *     port: "-1/-1",
 *     description: "description.",
 *     enable: "true",
 *     fwGroupId: "ALL",
 * });
 * const example3 = new tencentcloud.cfw.VpcPolicy("example3", {
 *     sourceContent: "3.3.3.3/0",
 *     sourceType: "net",
 *     destContent: "192.168.0.3",
 *     destType: "net",
 *     protocol: "ANY",
 *     ruleAction: "log",
 *     port: "-1/-1",
 *     description: "description.",
 *     enable: "true",
 *     fwGroupId: "ALL",
 * });
 * const example = new tencentcloud.cfw.VpcPolicyOrderConfig("example", {ruleUuidLists: [
 *     example3.uuid,
 *     example1.uuid,
 *     example2.uuid,
 * ]});
 * ```
 *
 * ## Import
 *
 * CFW vpc policy order config can be imported using the customId(like uuid or base64 string), e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Cfw/vpcPolicyOrderConfig:VpcPolicyOrderConfig example GedqV07VpNU0ob8LuOXw==
 * ```
 */
export class VpcPolicyOrderConfig extends pulumi.CustomResource {
    /**
     * Get an existing VpcPolicyOrderConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: VpcPolicyOrderConfigState, opts?: pulumi.CustomResourceOptions): VpcPolicyOrderConfig {
        return new VpcPolicyOrderConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Cfw/vpcPolicyOrderConfig:VpcPolicyOrderConfig';

    /**
     * Returns true if the given object is an instance of VpcPolicyOrderConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is VpcPolicyOrderConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === VpcPolicyOrderConfig.__pulumiType;
    }

    /**
     * The unique IDs of the rule, which is not required when you create a rule. The priority will be determined by the index position of the UUID in the list.
     */
    declare public readonly ruleUuidLists: pulumi.Output<number[]>;

    /**
     * Create a VpcPolicyOrderConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: VpcPolicyOrderConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: VpcPolicyOrderConfigArgs | VpcPolicyOrderConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as VpcPolicyOrderConfigState | undefined;
            resourceInputs["ruleUuidLists"] = state?.ruleUuidLists;
        } else {
            const args = argsOrState as VpcPolicyOrderConfigArgs | undefined;
            resourceInputs["ruleUuidLists"] = args?.ruleUuidLists;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(VpcPolicyOrderConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering VpcPolicyOrderConfig resources.
 */
export interface VpcPolicyOrderConfigState {
    /**
     * The unique IDs of the rule, which is not required when you create a rule. The priority will be determined by the index position of the UUID in the list.
     */
    ruleUuidLists?: pulumi.Input<pulumi.Input<number>[] | undefined>;
}

/**
 * The set of arguments for constructing a VpcPolicyOrderConfig resource.
 */
export interface VpcPolicyOrderConfigArgs {
    /**
     * The unique IDs of the rule, which is not required when you create a rule. The priority will be determined by the index position of the UUID in the list.
     */
    ruleUuidLists?: pulumi.Input<pulumi.Input<number>[] | undefined>;
}
