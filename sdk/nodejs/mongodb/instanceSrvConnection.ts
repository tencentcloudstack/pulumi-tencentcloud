// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to manage MongoDB instance SRV connection URL configuration.
 *
 * ## Example Usage
 *
 * ### Enable SRV connection with default domain
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.mongodb.InstanceSrvConnection("example", {instanceId: "cmgo-p8vnipr5"});
 * export const domain = example.domain;
 * ```
 *
 * ### Enable SRV connection with custom domain
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.mongodb.InstanceSrvConnection("example", {
 *     instanceId: "cmgo-p8vnipr5",
 *     domain: "exampleDomain",
 * });
 * ```
 *
 * ## Import
 *
 * MongoDB instance SRV connection can be imported using the instance id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Mongodb/instanceSrvConnection:InstanceSrvConnection example cmgo-p8vnipr5
 * ```
 */
export class InstanceSrvConnection extends pulumi.CustomResource {
    /**
     * Get an existing InstanceSrvConnection resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: InstanceSrvConnectionState, opts?: pulumi.CustomResourceOptions): InstanceSrvConnection {
        return new InstanceSrvConnection(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Mongodb/instanceSrvConnection:InstanceSrvConnection';

    /**
     * Returns true if the given object is an instance of InstanceSrvConnection.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is InstanceSrvConnection {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === InstanceSrvConnection.__pulumiType;
    }

    /**
     * Custom domain for SRV connection. If not specified during creation, the system will use a default domain. After creation, this field will be populated with the actual domain. To set or modify a custom domain, use this field.
     */
    declare public readonly domain: pulumi.Output<string>;
    /**
     * MongoDB instance ID, for example: cmgo-p8vnipr5.
     */
    declare public readonly instanceId: pulumi.Output<string>;

    /**
     * Create a InstanceSrvConnection resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: InstanceSrvConnectionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: InstanceSrvConnectionArgs | InstanceSrvConnectionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as InstanceSrvConnectionState | undefined;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["instanceId"] = state?.instanceId;
        } else {
            const args = argsOrState as InstanceSrvConnectionArgs | undefined;
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["domain"] = args?.domain;
            resourceInputs["instanceId"] = args?.instanceId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(InstanceSrvConnection.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering InstanceSrvConnection resources.
 */
export interface InstanceSrvConnectionState {
    /**
     * Custom domain for SRV connection. If not specified during creation, the system will use a default domain. After creation, this field will be populated with the actual domain. To set or modify a custom domain, use this field.
     */
    domain?: pulumi.Input<string | undefined>;
    /**
     * MongoDB instance ID, for example: cmgo-p8vnipr5.
     */
    instanceId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a InstanceSrvConnection resource.
 */
export interface InstanceSrvConnectionArgs {
    /**
     * Custom domain for SRV connection. If not specified during creation, the system will use a default domain. After creation, this field will be populated with the actual domain. To set or modify a custom domain, use this field.
     */
    domain?: pulumi.Input<string | undefined>;
    /**
     * MongoDB instance ID, for example: cmgo-p8vnipr5.
     */
    instanceId: pulumi.Input<string>;
}
