// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource to manage MongoDB audit service.
 *
 * ## Example Usage
 *
 * ### Full audit mode
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.mongodb.AuditService("example", {
 *     instanceId: "cmgo-5aqo4yf7",
 *     logExpireDay: 7,
 *     auditAll: true,
 * });
 * ```
 *
 * ### Rule-based audit mode
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.mongodb.AuditService("example", {
 *     instanceId: "cmgo-5aqo4yf7",
 *     logExpireDay: 30,
 *     auditAll: false,
 *     ruleFilters: [
 *         {
 *             type: "DB",
 *             compare: "EQ",
 *             values: ["testdb"],
 *         },
 *         {
 *             type: "User",
 *             compare: "EQ",
 *             values: ["admin"],
 *         },
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * MongoDB audit service can be imported using the instance id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Mongodb/auditService:AuditService example cmgo-5aqo4yf7
 * ```
 */
export class AuditService extends pulumi.CustomResource {
    /**
     * Get an existing AuditService resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AuditServiceState, opts?: pulumi.CustomResourceOptions): AuditService {
        return new AuditService(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Mongodb/auditService:AuditService';

    /**
     * Returns true if the given object is an instance of AuditService.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AuditService {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AuditService.__pulumiType;
    }

    /**
     * Whether to enable full audit. true: full audit, false: rule-based audit. When AuditAll is true, RuleFilters is not required.
     */
    declare public readonly auditAll: pulumi.Output<boolean>;
    /**
     * Instance ID, for example: cmgo-xfts****.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * Instance name.
     */
    declare public /*out*/ readonly instanceName: pulumi.Output<string>;
    /**
     * Audit log retention days. Valid values: 7, 30, 90, 180, 365, 1095, 1825.
     */
    declare public readonly logExpireDay: pulumi.Output<number>;
    /**
     * Audit log storage type.
     */
    declare public /*out*/ readonly logType: pulumi.Output<string>;
    /**
     * Audit filter rules. Only required when auditAll is false.
     */
    declare public readonly ruleFilters: pulumi.Output<outputs.Mongodb.AuditServiceRuleFilter[] | undefined>;

    /**
     * Create a AuditService resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AuditServiceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AuditServiceArgs | AuditServiceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AuditServiceState | undefined;
            resourceInputs["auditAll"] = state?.auditAll;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["instanceName"] = state?.instanceName;
            resourceInputs["logExpireDay"] = state?.logExpireDay;
            resourceInputs["logType"] = state?.logType;
            resourceInputs["ruleFilters"] = state?.ruleFilters;
        } else {
            const args = argsOrState as AuditServiceArgs | undefined;
            if (args?.auditAll === undefined && !opts.urn) {
                throw new Error("Missing required property 'auditAll'");
            }
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            if (args?.logExpireDay === undefined && !opts.urn) {
                throw new Error("Missing required property 'logExpireDay'");
            }
            resourceInputs["auditAll"] = args?.auditAll;
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["logExpireDay"] = args?.logExpireDay;
            resourceInputs["ruleFilters"] = args?.ruleFilters;
            resourceInputs["instanceName"] = undefined /*out*/;
            resourceInputs["logType"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AuditService.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AuditService resources.
 */
export interface AuditServiceState {
    /**
     * Whether to enable full audit. true: full audit, false: rule-based audit. When AuditAll is true, RuleFilters is not required.
     */
    auditAll?: pulumi.Input<boolean | undefined>;
    /**
     * Instance ID, for example: cmgo-xfts****.
     */
    instanceId?: pulumi.Input<string | undefined>;
    /**
     * Instance name.
     */
    instanceName?: pulumi.Input<string | undefined>;
    /**
     * Audit log retention days. Valid values: 7, 30, 90, 180, 365, 1095, 1825.
     */
    logExpireDay?: pulumi.Input<number | undefined>;
    /**
     * Audit log storage type.
     */
    logType?: pulumi.Input<string | undefined>;
    /**
     * Audit filter rules. Only required when auditAll is false.
     */
    ruleFilters?: pulumi.Input<pulumi.Input<inputs.Mongodb.AuditServiceRuleFilter>[] | undefined>;
}

/**
 * The set of arguments for constructing a AuditService resource.
 */
export interface AuditServiceArgs {
    /**
     * Whether to enable full audit. true: full audit, false: rule-based audit. When AuditAll is true, RuleFilters is not required.
     */
    auditAll: pulumi.Input<boolean>;
    /**
     * Instance ID, for example: cmgo-xfts****.
     */
    instanceId: pulumi.Input<string>;
    /**
     * Audit log retention days. Valid values: 7, 30, 90, 180, 365, 1095, 1825.
     */
    logExpireDay: pulumi.Input<number>;
    /**
     * Audit filter rules. Only required when auditAll is false.
     */
    ruleFilters?: pulumi.Input<pulumi.Input<inputs.Mongodb.AuditServiceRuleFilter>[] | undefined>;
}
