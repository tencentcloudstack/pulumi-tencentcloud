// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource to enable mongodb transparent data encryption
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const encryption = new tencentcloud.mongodb.InstanceTransparentDataEncryption("encryption", {
 *     instanceId: "cmgo-xxxxxx",
 *     kmsRegion: "ap-guangzhou",
 * });
 * ```
 *
 * ## Import
 *
 * mongodb transparent data encryption can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Mongodb/instanceTransparentDataEncryption:InstanceTransparentDataEncryption encryption ${instanceId}
 * ```
 */
export class InstanceTransparentDataEncryption extends pulumi.CustomResource {
    /**
     * Get an existing InstanceTransparentDataEncryption resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: InstanceTransparentDataEncryptionState, opts?: pulumi.CustomResourceOptions): InstanceTransparentDataEncryption {
        return new InstanceTransparentDataEncryption(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Mongodb/instanceTransparentDataEncryption:InstanceTransparentDataEncryption';

    /**
     * Returns true if the given object is an instance of InstanceTransparentDataEncryption.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is InstanceTransparentDataEncryption {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === InstanceTransparentDataEncryption.__pulumiType;
    }

    /**
     * Instance ID, for example: cmgo-p8vn ***. Currently supported general versions include: 4.4 and 5.0, but the cloud disk version is not currently supported.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    declare public readonly keyId: pulumi.Output<string | undefined>;
    /**
     * List of bound keys.
     */
    declare public /*out*/ readonly keyInfoLists: pulumi.Output<outputs.Mongodb.InstanceTransparentDataEncryptionKeyInfoList[]>;
    /**
     * The region where the Key Management Service (KMS) serves, such as ap-shanghai.
     */
    declare public readonly kmsRegion: pulumi.Output<string>;
    /**
     * Represents whether transparent encryption is turned on. Valid values:
     * - close: Not opened;
     * - open: It has been opened.
     */
    declare public /*out*/ readonly transparentDataEncryptionStatus: pulumi.Output<string>;

    /**
     * Create a InstanceTransparentDataEncryption resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: InstanceTransparentDataEncryptionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: InstanceTransparentDataEncryptionArgs | InstanceTransparentDataEncryptionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as InstanceTransparentDataEncryptionState | undefined;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["keyId"] = state?.keyId;
            resourceInputs["keyInfoLists"] = state?.keyInfoLists;
            resourceInputs["kmsRegion"] = state?.kmsRegion;
            resourceInputs["transparentDataEncryptionStatus"] = state?.transparentDataEncryptionStatus;
        } else {
            const args = argsOrState as InstanceTransparentDataEncryptionArgs | undefined;
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            if (args?.kmsRegion === undefined && !opts.urn) {
                throw new Error("Missing required property 'kmsRegion'");
            }
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["keyId"] = args?.keyId;
            resourceInputs["kmsRegion"] = args?.kmsRegion;
            resourceInputs["keyInfoLists"] = undefined /*out*/;
            resourceInputs["transparentDataEncryptionStatus"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(InstanceTransparentDataEncryption.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering InstanceTransparentDataEncryption resources.
 */
export interface InstanceTransparentDataEncryptionState {
    /**
     * Instance ID, for example: cmgo-p8vn ***. Currently supported general versions include: 4.4 and 5.0, but the cloud disk version is not currently supported.
     */
    instanceId?: pulumi.Input<string>;
    keyId?: pulumi.Input<string>;
    /**
     * List of bound keys.
     */
    keyInfoLists?: pulumi.Input<pulumi.Input<inputs.Mongodb.InstanceTransparentDataEncryptionKeyInfoList>[]>;
    /**
     * The region where the Key Management Service (KMS) serves, such as ap-shanghai.
     */
    kmsRegion?: pulumi.Input<string>;
    /**
     * Represents whether transparent encryption is turned on. Valid values:
     * - close: Not opened;
     * - open: It has been opened.
     */
    transparentDataEncryptionStatus?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a InstanceTransparentDataEncryption resource.
 */
export interface InstanceTransparentDataEncryptionArgs {
    /**
     * Instance ID, for example: cmgo-p8vn ***. Currently supported general versions include: 4.4 and 5.0, but the cloud disk version is not currently supported.
     */
    instanceId: pulumi.Input<string>;
    keyId?: pulumi.Input<string>;
    /**
     * The region where the Key Management Service (KMS) serves, such as ap-shanghai.
     */
    kmsRegion: pulumi.Input<string>;
}
