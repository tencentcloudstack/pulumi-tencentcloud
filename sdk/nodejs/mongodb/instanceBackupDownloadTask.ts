// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

export class InstanceBackupDownloadTask extends pulumi.CustomResource {
    /**
     * Get an existing InstanceBackupDownloadTask resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: InstanceBackupDownloadTaskState, opts?: pulumi.CustomResourceOptions): InstanceBackupDownloadTask {
        return new InstanceBackupDownloadTask(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Mongodb/instanceBackupDownloadTask:InstanceBackupDownloadTask';

    /**
     * Returns true if the given object is an instance of InstanceBackupDownloadTask.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is InstanceBackupDownloadTask {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === InstanceBackupDownloadTask.__pulumiType;
    }

    /**
     * The name of the backup file to be downloaded can be obtained through the DescribeDBBackups interface.
     */
    public readonly backupName!: pulumi.Output<string>;
    /**
     * Specifies the node names of replica sets to download or a list of shard names for sharded clusters.For example, the
     * replica set cmgo-p8vnipr5, example (fixed value): BackupSets.0=cmgo-p8vnipr5_0, the full amount of data can be
     * downloaded.For example, the sharded cluster cmgo-p8vnipr5, for example:
     * BackupSets.0=cmgo-p8vnipr5_0&amp;amp;BackupSets.1=cmgo-p8vnipr5_1, that is, to download the data of shard 0 and 1. If
     * the sharded cluster needs to be downloaded in full, please pass in the example. Full slice name.
     */
    public readonly backupSets!: pulumi.Output<outputs.Mongodb.InstanceBackupDownloadTaskBackupSet[]>;
    /**
     * Instance ID, the format is: cmgo-9d0p6umb.Same as the instance ID displayed in the cloud database console page.
     */
    public readonly instanceId!: pulumi.Output<string>;

    /**
     * Create a InstanceBackupDownloadTask resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: InstanceBackupDownloadTaskArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: InstanceBackupDownloadTaskArgs | InstanceBackupDownloadTaskState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as InstanceBackupDownloadTaskState | undefined;
            resourceInputs["backupName"] = state ? state.backupName : undefined;
            resourceInputs["backupSets"] = state ? state.backupSets : undefined;
            resourceInputs["instanceId"] = state ? state.instanceId : undefined;
        } else {
            const args = argsOrState as InstanceBackupDownloadTaskArgs | undefined;
            if ((!args || args.backupName === undefined) && !opts.urn) {
                throw new Error("Missing required property 'backupName'");
            }
            if ((!args || args.backupSets === undefined) && !opts.urn) {
                throw new Error("Missing required property 'backupSets'");
            }
            if ((!args || args.instanceId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["backupName"] = args ? args.backupName : undefined;
            resourceInputs["backupSets"] = args ? args.backupSets : undefined;
            resourceInputs["instanceId"] = args ? args.instanceId : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(InstanceBackupDownloadTask.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering InstanceBackupDownloadTask resources.
 */
export interface InstanceBackupDownloadTaskState {
    /**
     * The name of the backup file to be downloaded can be obtained through the DescribeDBBackups interface.
     */
    backupName?: pulumi.Input<string>;
    /**
     * Specifies the node names of replica sets to download or a list of shard names for sharded clusters.For example, the
     * replica set cmgo-p8vnipr5, example (fixed value): BackupSets.0=cmgo-p8vnipr5_0, the full amount of data can be
     * downloaded.For example, the sharded cluster cmgo-p8vnipr5, for example:
     * BackupSets.0=cmgo-p8vnipr5_0&amp;amp;BackupSets.1=cmgo-p8vnipr5_1, that is, to download the data of shard 0 and 1. If
     * the sharded cluster needs to be downloaded in full, please pass in the example. Full slice name.
     */
    backupSets?: pulumi.Input<pulumi.Input<inputs.Mongodb.InstanceBackupDownloadTaskBackupSet>[]>;
    /**
     * Instance ID, the format is: cmgo-9d0p6umb.Same as the instance ID displayed in the cloud database console page.
     */
    instanceId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a InstanceBackupDownloadTask resource.
 */
export interface InstanceBackupDownloadTaskArgs {
    /**
     * The name of the backup file to be downloaded can be obtained through the DescribeDBBackups interface.
     */
    backupName: pulumi.Input<string>;
    /**
     * Specifies the node names of replica sets to download or a list of shard names for sharded clusters.For example, the
     * replica set cmgo-p8vnipr5, example (fixed value): BackupSets.0=cmgo-p8vnipr5_0, the full amount of data can be
     * downloaded.For example, the sharded cluster cmgo-p8vnipr5, for example:
     * BackupSets.0=cmgo-p8vnipr5_0&amp;amp;BackupSets.1=cmgo-p8vnipr5_1, that is, to download the data of shard 0 and 1. If
     * the sharded cluster needs to be downloaded in full, please pass in the example. Full slice name.
     */
    backupSets: pulumi.Input<pulumi.Input<inputs.Mongodb.InstanceBackupDownloadTaskBackupSet>[]>;
    /**
     * Instance ID, the format is: cmgo-9d0p6umb.Same as the instance ID displayed in the cloud database console page.
     */
    instanceId: pulumi.Input<string>;
}
