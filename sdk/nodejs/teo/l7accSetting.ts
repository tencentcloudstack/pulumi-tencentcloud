// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a teo l7AccSetting
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const teoL7AccSetting = new tencentcloud.teo.L7AccSetting("teo_l7_acc_setting", {
 *     zoneId: "zone-36bjhygh1bxe",
 *     zoneConfig: {
 *         accelerateMainland: {
 *             "switch": "on",
 *         },
 *         cache: {
 *             customTime: {
 *                 cacheTime: 2592000,
 *                 "switch": "off",
 *             },
 *             followOrigin: {
 *                 defaultCache: "off",
 *                 defaultCacheStrategy: "on",
 *                 defaultCacheTime: 0,
 *                 "switch": "on",
 *             },
 *             noCache: {
 *                 "switch": "off",
 *             },
 *         },
 *         cacheKey: {
 *             fullUrlCache: "on",
 *             ignoreCase: "off",
 *             queryString: {
 *                 action: "includeCustom",
 *                 "switch": "off",
 *             },
 *         },
 *         cachePrefresh: {
 *             cacheTimePercent: 90,
 *             "switch": "off",
 *         },
 *         clientIpCountry: {
 *             "switch": "off",
 *         },
 *         clientIpHeader: {
 *             "switch": "off",
 *         },
 *         compression: {
 *             algorithms: [
 *                 "brotli",
 *                 "gzip",
 *             ],
 *             "switch": "on",
 *         },
 *         forceRedirectHttps: {
 *             redirectStatusCode: 302,
 *             "switch": "off",
 *         },
 *         grpc: {
 *             "switch": "off",
 *         },
 *         hsts: {
 *             includeSubDomains: "off",
 *             preload: "off",
 *             "switch": "off",
 *             timeout: 0,
 *         },
 *         http2: {
 *             "switch": "off",
 *         },
 *         ipv6: {
 *             "switch": "off",
 *         },
 *         maxAge: {
 *             cacheTime: 600,
 *             followOrigin: "on",
 *         },
 *         ocspStapling: {
 *             "switch": "off",
 *         },
 *         offlineCache: {
 *             "switch": "on",
 *         },
 *         postMaxSize: {
 *             maxSize: 838860800,
 *             "switch": "on",
 *         },
 *         quic: {
 *             "switch": "off",
 *         },
 *         smartRouting: {
 *             "switch": "off",
 *         },
 *         standardDebug: {
 *             allowClientIpLists: [],
 *             expires: "1969-12-31T16:00:00Z",
 *             "switch": "off",
 *         },
 *         tlsConfig: {
 *             cipherSuite: "loose-v2023",
 *             versions: [
 *                 "TLSv1",
 *                 "TLSv1.1",
 *                 "TLSv1.2",
 *                 "TLSv1.3",
 *             ],
 *         },
 *         upstreamHttp2: {
 *             "switch": "off",
 *         },
 *         webSocket: {
 *             "switch": "off",
 *             timeout: 30,
 *         },
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * teo l7_acc_setting can be imported using the zone_id, e.g.
 *
 * `
 *
 * ```sh
 * $ pulumi import tencentcloud:Teo/l7AccSetting:L7AccSetting teo_l7_acc_setting zone-297z8rf93cfw
 * ```
 *
 * `
 */
export class L7AccSetting extends pulumi.CustomResource {
    /**
     * Get an existing L7AccSetting resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: L7AccSettingState, opts?: pulumi.CustomResourceOptions): L7AccSetting {
        return new L7AccSetting(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Teo/l7AccSetting:L7AccSetting';

    /**
     * Returns true if the given object is an instance of L7AccSetting.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is L7AccSetting {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === L7AccSetting.__pulumiType;
    }

    /**
     * Site acceleration global configuration. the settings in this parameter will apply to all domain names under the site. you only need to modify the required settings directly, and other settings not passed in will remain unchanged.
     */
    declare public readonly zoneConfig: pulumi.Output<outputs.Teo.L7AccSettingZoneConfig>;
    /**
     * Zone id.
     */
    declare public readonly zoneId: pulumi.Output<string>;
    /**
     * Zone name.
     */
    declare public /*out*/ readonly zoneName: pulumi.Output<string>;

    /**
     * Create a L7AccSetting resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: L7AccSettingArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: L7AccSettingArgs | L7AccSettingState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as L7AccSettingState | undefined;
            resourceInputs["zoneConfig"] = state?.zoneConfig;
            resourceInputs["zoneId"] = state?.zoneId;
            resourceInputs["zoneName"] = state?.zoneName;
        } else {
            const args = argsOrState as L7AccSettingArgs | undefined;
            if (args?.zoneConfig === undefined && !opts.urn) {
                throw new Error("Missing required property 'zoneConfig'");
            }
            if (args?.zoneId === undefined && !opts.urn) {
                throw new Error("Missing required property 'zoneId'");
            }
            resourceInputs["zoneConfig"] = args?.zoneConfig;
            resourceInputs["zoneId"] = args?.zoneId;
            resourceInputs["zoneName"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(L7AccSetting.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering L7AccSetting resources.
 */
export interface L7AccSettingState {
    /**
     * Site acceleration global configuration. the settings in this parameter will apply to all domain names under the site. you only need to modify the required settings directly, and other settings not passed in will remain unchanged.
     */
    zoneConfig?: pulumi.Input<inputs.Teo.L7AccSettingZoneConfig>;
    /**
     * Zone id.
     */
    zoneId?: pulumi.Input<string>;
    /**
     * Zone name.
     */
    zoneName?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a L7AccSetting resource.
 */
export interface L7AccSettingArgs {
    /**
     * Site acceleration global configuration. the settings in this parameter will apply to all domain names under the site. you only need to modify the required settings directly, and other settings not passed in will remain unchanged.
     */
    zoneConfig: pulumi.Input<inputs.Teo.L7AccSettingZoneConfig>;
    /**
     * Zone id.
     */
    zoneId: pulumi.Input<string>;
}
