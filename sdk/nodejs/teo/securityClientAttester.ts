// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a TEO security client attester.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.teo.SecurityClientAttester("example", {
 *     zoneId: "zone-3fkff38fyw8s",
 *     clientAttesters: {
 *         name: "tf-example",
 *         attesterSource: "TC-RCE",
 *         attesterDuration: "300s",
 *         tcRceOption: {
 *             channel: "12399223",
 *             region: "ap-beijing",
 *         },
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * TEO security client attester can be imported using the zoneId#clientAttesterId, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Teo/securityClientAttester:SecurityClientAttester example zone-3fkff38fyw8s#attest-0000361666
 * ```
 */
export class SecurityClientAttester extends pulumi.CustomResource {
    /**
     * Get an existing SecurityClientAttester resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecurityClientAttesterState, opts?: pulumi.CustomResourceOptions): SecurityClientAttester {
        return new SecurityClientAttester(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Teo/securityClientAttester:SecurityClientAttester';

    /**
     * Returns true if the given object is an instance of SecurityClientAttester.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecurityClientAttester {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecurityClientAttester.__pulumiType;
    }

    /**
     * Client attester configuration. Only one attester is allowed per request.
     */
    declare public readonly clientAttesters: pulumi.Output<outputs.Teo.SecurityClientAttesterClientAttesters>;
    /**
     * Site ID.
     */
    declare public readonly zoneId: pulumi.Output<string>;

    /**
     * Create a SecurityClientAttester resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecurityClientAttesterArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecurityClientAttesterArgs | SecurityClientAttesterState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecurityClientAttesterState | undefined;
            resourceInputs["clientAttesters"] = state?.clientAttesters;
            resourceInputs["zoneId"] = state?.zoneId;
        } else {
            const args = argsOrState as SecurityClientAttesterArgs | undefined;
            if (args?.clientAttesters === undefined && !opts.urn) {
                throw new Error("Missing required property 'clientAttesters'");
            }
            if (args?.zoneId === undefined && !opts.urn) {
                throw new Error("Missing required property 'zoneId'");
            }
            resourceInputs["clientAttesters"] = args?.clientAttesters;
            resourceInputs["zoneId"] = args?.zoneId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecurityClientAttester.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecurityClientAttester resources.
 */
export interface SecurityClientAttesterState {
    /**
     * Client attester configuration. Only one attester is allowed per request.
     */
    clientAttesters?: pulumi.Input<inputs.Teo.SecurityClientAttesterClientAttesters | undefined>;
    /**
     * Site ID.
     */
    zoneId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SecurityClientAttester resource.
 */
export interface SecurityClientAttesterArgs {
    /**
     * Client attester configuration. Only one attester is allowed per request.
     */
    clientAttesters: pulumi.Input<inputs.Teo.SecurityClientAttesterClientAttesters>;
    /**
     * Site ID.
     */
    zoneId: pulumi.Input<string>;
}
