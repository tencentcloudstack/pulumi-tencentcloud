// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a CDB mysql backup
 *
 * > **NOTE:** Concurrent backups are not supported; multiple backups must be executed sequentially.
 *
 * Documentation: https://cloud.tencent.com/document/product/236/35172
 *
 * TencentDB for MySQL (Local Disk)—supporting 2-node, 3-node, and 4-node architectures—supports two backup types:
 * `Physical backup`: A full copy of physical data (supported by automatic backups).
 * `Logical backup`: Backup via SQL statements (supported by both manual and automatic backups).
 *
 * TencentDB for MySQL Single-Node (Cloud Disk) and Cloud Disk editions support snapshot backups:
 * `Snapshot backup`: Backup performed by creating snapshots of disks at the storage layer (supported by both automatic and manual backups).
 *
 * ## Example Usage
 *
 * ### Create a physical full backup
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const zones = tencentcloud.availability.getZonesByProduct({
 *     product: "cdb",
 * });
 * const vpc = new tencentcloud.vpc.Instance("vpc", {
 *     name: "vpc-mysql",
 *     cidrBlock: "10.0.0.0/16",
 * });
 * const subnet = new tencentcloud.subnet.Instance("subnet", {
 *     availabilityZone: zones.then(zones => zones.zones?.[0]?.name),
 *     name: "subnet-mysql",
 *     vpcId: vpc.id,
 *     cidrBlock: "10.0.0.0/16",
 *     isMulticast: false,
 * });
 * const securityGroup = new tencentcloud.security.Group("security_group", {
 *     name: "sg-mysql",
 *     description: "mysql test",
 * });
 * const example = new tencentcloud.mysql.Instance("example", {
 *     internetService: 1,
 *     engineVersion: "5.7",
 *     chargeType: "POSTPAID",
 *     rootPassword: "PassWord123",
 *     slaveDeployMode: 0,
 *     availabilityZone: zones.then(zones => zones.zones?.[0]?.name),
 *     slaveSyncMode: 1,
 *     instanceName: "tf-example-mysql",
 *     memSize: 4000,
 *     volumeSize: 200,
 *     vpcId: vpc.id,
 *     subnetId: subnet.id,
 *     intranetPort: 3306,
 *     securityGroups: [securityGroup.id],
 *     tags: {
 *         name: "test",
 *     },
 *     parameters: {
 *         character_set_server: "utf8",
 *         max_connections: "1000",
 *     },
 * });
 * const exampleBackup = new tencentcloud.mysql.Backup("example", {
 *     instanceId: example.id,
 *     backupMethod: "physical",
 *     manualBackupName: "tf-example-backup",
 *     encryptionFlag: "off",
 * });
 * ```
 *
 * ### Create a logical backup with specific database and table
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const logical = new tencentcloud.mysql.Backup("logical", {
 *     instanceId: example.id,
 *     backupMethod: "logical",
 *     manualBackupName: "tf-logical-backup",
 *     backupDbTableLists: [
 *         {
 *             database: "db1",
 *             table: "tb1",
 *         },
 *         {
 *             database: "db2",
 *         },
 *     ],
 * });
 * ```
 */
export class Backup extends pulumi.CustomResource {
    /**
     * Get an existing Backup resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: BackupState, opts?: pulumi.CustomResourceOptions): Backup {
        return new Backup(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Mysql/backup:Backup';

    /**
     * Returns true if the given object is an instance of Backup.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Backup {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Backup.__pulumiType;
    }

    /**
     * List of databases and tables to backup. Only valid when `backupMethod` is `logical`. The specified databases and tables must exist, otherwise backup may fail.
     */
    declare public readonly backupDbTableLists: pulumi.Output<outputs.Mysql.BackupBackupDbTableList[] | undefined>;
    /**
     * ID of the backup task.
     */
    declare public /*out*/ readonly backupId: pulumi.Output<number>;
    /**
     * Target backup method. Supported values include: `logical` - logical cold backup, `physical` - physical cold backup, `snapshot` - snapshot backup. Basic edition instances only support snapshot backup.
     */
    declare public readonly backupMethod: pulumi.Output<string>;
    /**
     * Whether to encrypt physical backup. Supported values include: `on` - yes, `off` - no. Only valid when `backupMethod` is `physical`. If not specified, the instance's default backup encryption policy is used.
     */
    declare public readonly encryptionFlag: pulumi.Output<string | undefined>;
    /**
     * Instance ID, such as `cdb-c1nl9rpv`. It is identical to the instance ID displayed in the database console page.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * Internet download URL of the backup file.
     */
    declare public /*out*/ readonly internetUrl: pulumi.Output<string>;
    /**
     * Intranet download URL of the backup file.
     */
    declare public /*out*/ readonly intranetUrl: pulumi.Output<string>;
    /**
     * Manual backup alias. Maximum length is 60 characters.
     */
    declare public readonly manualBackupName: pulumi.Output<string | undefined>;

    /**
     * Create a Backup resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BackupArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: BackupArgs | BackupState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as BackupState | undefined;
            resourceInputs["backupDbTableLists"] = state?.backupDbTableLists;
            resourceInputs["backupId"] = state?.backupId;
            resourceInputs["backupMethod"] = state?.backupMethod;
            resourceInputs["encryptionFlag"] = state?.encryptionFlag;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["internetUrl"] = state?.internetUrl;
            resourceInputs["intranetUrl"] = state?.intranetUrl;
            resourceInputs["manualBackupName"] = state?.manualBackupName;
        } else {
            const args = argsOrState as BackupArgs | undefined;
            if (args?.backupMethod === undefined && !opts.urn) {
                throw new Error("Missing required property 'backupMethod'");
            }
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["backupDbTableLists"] = args?.backupDbTableLists;
            resourceInputs["backupMethod"] = args?.backupMethod;
            resourceInputs["encryptionFlag"] = args?.encryptionFlag;
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["manualBackupName"] = args?.manualBackupName;
            resourceInputs["backupId"] = undefined /*out*/;
            resourceInputs["internetUrl"] = undefined /*out*/;
            resourceInputs["intranetUrl"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Backup.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Backup resources.
 */
export interface BackupState {
    /**
     * List of databases and tables to backup. Only valid when `backupMethod` is `logical`. The specified databases and tables must exist, otherwise backup may fail.
     */
    backupDbTableLists?: pulumi.Input<pulumi.Input<inputs.Mysql.BackupBackupDbTableList>[] | undefined>;
    /**
     * ID of the backup task.
     */
    backupId?: pulumi.Input<number | undefined>;
    /**
     * Target backup method. Supported values include: `logical` - logical cold backup, `physical` - physical cold backup, `snapshot` - snapshot backup. Basic edition instances only support snapshot backup.
     */
    backupMethod?: pulumi.Input<string | undefined>;
    /**
     * Whether to encrypt physical backup. Supported values include: `on` - yes, `off` - no. Only valid when `backupMethod` is `physical`. If not specified, the instance's default backup encryption policy is used.
     */
    encryptionFlag?: pulumi.Input<string | undefined>;
    /**
     * Instance ID, such as `cdb-c1nl9rpv`. It is identical to the instance ID displayed in the database console page.
     */
    instanceId?: pulumi.Input<string | undefined>;
    /**
     * Internet download URL of the backup file.
     */
    internetUrl?: pulumi.Input<string | undefined>;
    /**
     * Intranet download URL of the backup file.
     */
    intranetUrl?: pulumi.Input<string | undefined>;
    /**
     * Manual backup alias. Maximum length is 60 characters.
     */
    manualBackupName?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a Backup resource.
 */
export interface BackupArgs {
    /**
     * List of databases and tables to backup. Only valid when `backupMethod` is `logical`. The specified databases and tables must exist, otherwise backup may fail.
     */
    backupDbTableLists?: pulumi.Input<pulumi.Input<inputs.Mysql.BackupBackupDbTableList>[] | undefined>;
    /**
     * Target backup method. Supported values include: `logical` - logical cold backup, `physical` - physical cold backup, `snapshot` - snapshot backup. Basic edition instances only support snapshot backup.
     */
    backupMethod: pulumi.Input<string>;
    /**
     * Whether to encrypt physical backup. Supported values include: `on` - yes, `off` - no. Only valid when `backupMethod` is `physical`. If not specified, the instance's default backup encryption policy is used.
     */
    encryptionFlag?: pulumi.Input<string | undefined>;
    /**
     * Instance ID, such as `cdb-c1nl9rpv`. It is identical to the instance ID displayed in the database console page.
     */
    instanceId: pulumi.Input<string>;
    /**
     * Manual backup alias. Maximum length is 60 characters.
     */
    manualBackupName?: pulumi.Input<string | undefined>;
}
