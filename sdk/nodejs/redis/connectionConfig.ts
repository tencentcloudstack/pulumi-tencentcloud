// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a redis connection config
 *
 * ## Example Usage
 *
 * ### Modify the maximum number of connections and maximum network throughput of an instance
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const zone = tencentcloud.Redis.getZoneConfig({
 *     typeId: 7,
 * });
 * const vpc = new tencentcloud.vpc.Instance("vpc", {
 *     cidrBlock: "10.0.0.0/16",
 *     name: "tf_redis_vpc",
 * });
 * const subnet = new tencentcloud.subnet.Instance("subnet", {
 *     vpcId: vpc.id,
 *     availabilityZone: zone.then(zone => zone.lists?.[0]?.zone),
 *     name: "tf_redis_subnet",
 *     cidrBlock: "10.0.1.0/24",
 * });
 * const example = new tencentcloud.redis.Instance("example", {
 *     availabilityZone: zone.then(zone => zone.lists?.[0]?.zone),
 *     typeId: zone.then(zone => zone.lists?.[0]?.typeId),
 *     password: "Password@123",
 *     memSize: 8192,
 *     redisShardNum: zone.then(zone => zone.lists?.[0]?.redisShardNums?.[0]),
 *     redisReplicasNum: zone.then(zone => zone.lists?.[0]?.redisReplicasNums?.[0]),
 *     name: "tf_example",
 *     port: 6379,
 *     vpcId: vpc.id,
 *     subnetId: subnet.id,
 * });
 * const exampleConnectionConfig = new tencentcloud.redis.ConnectionConfig("example", {
 *     instanceId: example.id,
 *     clientLimit: 20000,
 *     addBandwidth: 30,
 * });
 * ```
 *
 * ## Import
 *
 * redis connection config can be imported, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Redis/connectionConfig:ConnectionConfig example crs-cqdfdzvt
 * ```
 */
export class ConnectionConfig extends pulumi.CustomResource {
    /**
     * Get an existing ConnectionConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ConnectionConfigState, opts?: pulumi.CustomResourceOptions): ConnectionConfig {
        return new ConnectionConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Redis/connectionConfig:ConnectionConfig';

    /**
     * Returns true if the given object is an instance of ConnectionConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ConnectionConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ConnectionConfig.__pulumiType;
    }

    /**
     * Refers to the additional bandwidth of the instance. When the standard bandwidth does not meet the demand, the user can increase the bandwidth by himself. When the read-only copy is enabled, the total bandwidth of the instance = additional bandwidth * number of fragments + standard bandwidth * number of fragments * Max ([number of read-only replicas, 1] ), the number of shards in the standard architecture = 1, and when read-only replicas are not enabled, the total bandwidth of the instance = additional bandwidth * number of shards + standard bandwidth * number of shards, and the number of shards in the standard architecture = 1.
     */
    declare public readonly addBandwidth: pulumi.Output<number>;
    /**
     * standard bandwidth. Refers to the bandwidth allocated by the system to each node when an instance is purchased.
     */
    declare public /*out*/ readonly baseBandwidth: pulumi.Output<number>;
    /**
     * The total number of connections per shard.If read-only replicas are not enabled, the lower limit is 10,000 and the upper limit is 40,000.When you enable read-only replicas, the minimum limit is 10,000 and the upper limit is 10,000 * (the number of read replicas +3).
     */
    declare public readonly clientLimit: pulumi.Output<number | undefined>;
    /**
     * The ID of instance.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * Additional bandwidth is capped.
     */
    declare public /*out*/ readonly maxAddBandwidth: pulumi.Output<number>;
    /**
     * Additional bandwidth sets the lower limit.
     */
    declare public /*out*/ readonly minAddBandwidth: pulumi.Output<number>;
    /**
     * Total bandwidth of the instance = additional bandwidth * number of shards + standard bandwidth * number of shards * (number of primary nodes + number of read-only replica nodes), the number of shards of the standard architecture = 1, in Mb/s.
     */
    declare public /*out*/ readonly totalBandwidth: pulumi.Output<number>;

    /**
     * Create a ConnectionConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ConnectionConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ConnectionConfigArgs | ConnectionConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ConnectionConfigState | undefined;
            resourceInputs["addBandwidth"] = state?.addBandwidth;
            resourceInputs["baseBandwidth"] = state?.baseBandwidth;
            resourceInputs["clientLimit"] = state?.clientLimit;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["maxAddBandwidth"] = state?.maxAddBandwidth;
            resourceInputs["minAddBandwidth"] = state?.minAddBandwidth;
            resourceInputs["totalBandwidth"] = state?.totalBandwidth;
        } else {
            const args = argsOrState as ConnectionConfigArgs | undefined;
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["addBandwidth"] = args?.addBandwidth;
            resourceInputs["clientLimit"] = args?.clientLimit;
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["baseBandwidth"] = undefined /*out*/;
            resourceInputs["maxAddBandwidth"] = undefined /*out*/;
            resourceInputs["minAddBandwidth"] = undefined /*out*/;
            resourceInputs["totalBandwidth"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ConnectionConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ConnectionConfig resources.
 */
export interface ConnectionConfigState {
    /**
     * Refers to the additional bandwidth of the instance. When the standard bandwidth does not meet the demand, the user can increase the bandwidth by himself. When the read-only copy is enabled, the total bandwidth of the instance = additional bandwidth * number of fragments + standard bandwidth * number of fragments * Max ([number of read-only replicas, 1] ), the number of shards in the standard architecture = 1, and when read-only replicas are not enabled, the total bandwidth of the instance = additional bandwidth * number of shards + standard bandwidth * number of shards, and the number of shards in the standard architecture = 1.
     */
    addBandwidth?: pulumi.Input<number>;
    /**
     * standard bandwidth. Refers to the bandwidth allocated by the system to each node when an instance is purchased.
     */
    baseBandwidth?: pulumi.Input<number>;
    /**
     * The total number of connections per shard.If read-only replicas are not enabled, the lower limit is 10,000 and the upper limit is 40,000.When you enable read-only replicas, the minimum limit is 10,000 and the upper limit is 10,000 * (the number of read replicas +3).
     */
    clientLimit?: pulumi.Input<number>;
    /**
     * The ID of instance.
     */
    instanceId?: pulumi.Input<string>;
    /**
     * Additional bandwidth is capped.
     */
    maxAddBandwidth?: pulumi.Input<number>;
    /**
     * Additional bandwidth sets the lower limit.
     */
    minAddBandwidth?: pulumi.Input<number>;
    /**
     * Total bandwidth of the instance = additional bandwidth * number of shards + standard bandwidth * number of shards * (number of primary nodes + number of read-only replica nodes), the number of shards of the standard architecture = 1, in Mb/s.
     */
    totalBandwidth?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a ConnectionConfig resource.
 */
export interface ConnectionConfigArgs {
    /**
     * Refers to the additional bandwidth of the instance. When the standard bandwidth does not meet the demand, the user can increase the bandwidth by himself. When the read-only copy is enabled, the total bandwidth of the instance = additional bandwidth * number of fragments + standard bandwidth * number of fragments * Max ([number of read-only replicas, 1] ), the number of shards in the standard architecture = 1, and when read-only replicas are not enabled, the total bandwidth of the instance = additional bandwidth * number of shards + standard bandwidth * number of shards, and the number of shards in the standard architecture = 1.
     */
    addBandwidth?: pulumi.Input<number>;
    /**
     * The total number of connections per shard.If read-only replicas are not enabled, the lower limit is 10,000 and the upper limit is 40,000.When you enable read-only replicas, the minimum limit is 10,000 and the upper limit is 10,000 * (the number of read replicas +3).
     */
    clientLimit?: pulumi.Input<number>;
    /**
     * The ID of instance.
     */
    instanceId: pulumi.Input<string>;
}
