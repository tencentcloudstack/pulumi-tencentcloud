// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a redis replicate attachment
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const zone = tencentcloud.Redis.getZoneConfig({
 *     typeId: 7,
 *     region: "ap-guangzhou",
 * });
 * const vpc = new tencentcloud.vpc.Instance("vpc", {
 *     cidrBlock: "10.0.0.0/16",
 *     name: "tf_redis_vpc",
 * });
 * const subnet = new tencentcloud.subnet.Instance("subnet", {
 *     vpcId: vpc.id,
 *     availabilityZone: zone.then(zone => zone.lists?.[2]?.zone),
 *     name: "tf_redis_subnet",
 *     cidrBlock: "10.0.1.0/24",
 * });
 * const securityGroup = new tencentcloud.security.Group("security_group", {name: "tf-redis-sg"});
 * const example = new tencentcloud.index.SecurityGroupLiteRule("example", {
 *     securityGroupId: securityGroup.id,
 *     ingresses: [
 *         "ACCEPT#192.168.1.0/24#80#TCP",
 *         "DROP#8.8.8.8#80,90#UDP",
 *         "DROP#0.0.0.0/0#80-90#TCP",
 *     ],
 *     egresses: [
 *         "ACCEPT#192.168.0.0/16#ALL#TCP",
 *         "ACCEPT#10.0.0.0/8#ALL#ICMP",
 *         "DROP#0.0.0.0/0#ALL#ALL",
 *     ],
 * });
 * const example1 = new tencentcloud.redis.Instance("example1", {
 *     availabilityZone: zone.then(zone => zone.lists?.[2]?.zone),
 *     typeId: zone.then(zone => zone.lists?.[2]?.typeId),
 *     password: "Password@123",
 *     memSize: 8192,
 *     redisShardNum: zone.then(zone => zone.lists?.[2]?.redisShardNums?.[0]),
 *     redisReplicasNum: zone.then(zone => zone.lists?.[2]?.redisReplicasNums?.[0]),
 *     name: "tf_example1",
 *     port: 6379,
 *     vpcId: vpc.id,
 *     subnetId: subnet.id,
 *     securityGroups: [securityGroup.id],
 * });
 * const example2 = new tencentcloud.redis.Instance("example2", {
 *     availabilityZone: zone.then(zone => zone.lists?.[2]?.zone),
 *     typeId: zone.then(zone => zone.lists?.[2]?.typeId),
 *     password: "Password@123",
 *     memSize: 8192,
 *     redisShardNum: zone.then(zone => zone.lists?.[2]?.redisShardNums?.[0]),
 *     redisReplicasNum: zone.then(zone => zone.lists?.[2]?.redisReplicasNums?.[0]),
 *     name: "tf_example2",
 *     port: 6379,
 *     vpcId: vpc.id,
 *     subnetId: subnet.id,
 *     securityGroups: [securityGroup.id],
 * });
 * const exampleReplicateAttachment = new tencentcloud.redis.ReplicateAttachment("example", {
 *     groupId: "crs-rpl-orfiwmn5",
 *     masterInstanceId: example1.id,
 *     instanceIds: [example2.id],
 * });
 * ```
 *
 * ## Import
 *
 * redis replicate attachment can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Redis/replicateAttachment:ReplicateAttachment replicate_attachment crs-rpl-orfiwmn5
 * ```
 */
export class ReplicateAttachment extends pulumi.CustomResource {
    /**
     * Get an existing ReplicateAttachment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ReplicateAttachmentState, opts?: pulumi.CustomResourceOptions): ReplicateAttachment {
        return new ReplicateAttachment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Redis/replicateAttachment:ReplicateAttachment';

    /**
     * Returns true if the given object is an instance of ReplicateAttachment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ReplicateAttachment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ReplicateAttachment.__pulumiType;
    }

    /**
     * The ID of group.
     */
    declare public readonly groupId: pulumi.Output<string>;
    /**
     * All instance ids of the replication group.
     */
    declare public readonly instanceIds: pulumi.Output<string[]>;
    /**
     * The ID of master instance.
     */
    declare public readonly masterInstanceId: pulumi.Output<string>;

    /**
     * Create a ReplicateAttachment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ReplicateAttachmentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ReplicateAttachmentArgs | ReplicateAttachmentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ReplicateAttachmentState | undefined;
            resourceInputs["groupId"] = state?.groupId;
            resourceInputs["instanceIds"] = state?.instanceIds;
            resourceInputs["masterInstanceId"] = state?.masterInstanceId;
        } else {
            const args = argsOrState as ReplicateAttachmentArgs | undefined;
            if (args?.groupId === undefined && !opts.urn) {
                throw new Error("Missing required property 'groupId'");
            }
            if (args?.instanceIds === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceIds'");
            }
            if (args?.masterInstanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'masterInstanceId'");
            }
            resourceInputs["groupId"] = args?.groupId;
            resourceInputs["instanceIds"] = args?.instanceIds;
            resourceInputs["masterInstanceId"] = args?.masterInstanceId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ReplicateAttachment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ReplicateAttachment resources.
 */
export interface ReplicateAttachmentState {
    /**
     * The ID of group.
     */
    groupId?: pulumi.Input<string>;
    /**
     * All instance ids of the replication group.
     */
    instanceIds?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The ID of master instance.
     */
    masterInstanceId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a ReplicateAttachment resource.
 */
export interface ReplicateAttachmentArgs {
    /**
     * The ID of group.
     */
    groupId: pulumi.Input<string>;
    /**
     * All instance ids of the replication group.
     */
    instanceIds: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The ID of master instance.
     */
    masterInstanceId: pulumi.Input<string>;
}
