// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource for bind receivers to a policy group resource.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const groups = tencentcloud.Cam.getGroups({});
 * const group = new tencentcloud.index.MonitorPolicyGroup("group", {
 *     groupName: "nice_group",
 *     policyViewName: "cvm_device",
 *     remark: "this is a test policy group",
 *     conditions: [{
 *         metricId: 33,
 *         alarmNotifyType: 1,
 *         alarmNotifyPeriod: 600,
 *         calcType: 1,
 *         calcValue: 3,
 *         calcPeriod: 300,
 *         continuePeriod: 2,
 *     }],
 * });
 * const receiver = new tencentcloud.monitor.BindingReceiver("receiver", {
 *     groupId: group.id,
 *     receivers: {
 *         startTime: 0,
 *         endTime: 86399,
 *         notifyWays: ["SMS"],
 *         receiverType: "group",
 *         receiverGroupLists: [groups.then(groups => groups.groupLists?.[0]?.groupId)],
 *         receiveLanguage: "en-US",
 *     },
 * });
 * ```
 */
export class BindingReceiver extends pulumi.CustomResource {
    /**
     * Get an existing BindingReceiver resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: BindingReceiverState, opts?: pulumi.CustomResourceOptions): BindingReceiver {
        return new BindingReceiver(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Monitor/bindingReceiver:BindingReceiver';

    /**
     * Returns true if the given object is an instance of BindingReceiver.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BindingReceiver {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BindingReceiver.__pulumiType;
    }

    /**
     * Policy group ID for binding receivers.
     */
    declare public readonly groupId: pulumi.Output<number>;
    /**
     * A list of receivers(will overwrite the configuration of the server or other resources). Each element contains the following attributes:
     */
    declare public readonly receivers: pulumi.Output<outputs.Monitor.BindingReceiverReceivers | undefined>;

    /**
     * Create a BindingReceiver resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BindingReceiverArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: BindingReceiverArgs | BindingReceiverState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as BindingReceiverState | undefined;
            resourceInputs["groupId"] = state?.groupId;
            resourceInputs["receivers"] = state?.receivers;
        } else {
            const args = argsOrState as BindingReceiverArgs | undefined;
            if (args?.groupId === undefined && !opts.urn) {
                throw new Error("Missing required property 'groupId'");
            }
            resourceInputs["groupId"] = args?.groupId;
            resourceInputs["receivers"] = args?.receivers;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(BindingReceiver.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering BindingReceiver resources.
 */
export interface BindingReceiverState {
    /**
     * Policy group ID for binding receivers.
     */
    groupId?: pulumi.Input<number>;
    /**
     * A list of receivers(will overwrite the configuration of the server or other resources). Each element contains the following attributes:
     */
    receivers?: pulumi.Input<inputs.Monitor.BindingReceiverReceivers>;
}

/**
 * The set of arguments for constructing a BindingReceiver resource.
 */
export interface BindingReceiverArgs {
    /**
     * Policy group ID for binding receivers.
     */
    groupId: pulumi.Input<number>;
    /**
     * A list of receivers(will overwrite the configuration of the server or other resources). Each element contains the following attributes:
     */
    receivers?: pulumi.Input<inputs.Monitor.BindingReceiverReceivers>;
}
