// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import { input as inputs, output as outputs } from "../types";
import * as utilities from "../utilities";

export class BindingAlarmReceiver extends pulumi.CustomResource {
    /**
     * Get an existing BindingAlarmReceiver resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: BindingAlarmReceiverState, opts?: pulumi.CustomResourceOptions): BindingAlarmReceiver {
        return new BindingAlarmReceiver(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Monitor/bindingAlarmReceiver:BindingAlarmReceiver';

    /**
     * Returns true if the given object is an instance of BindingAlarmReceiver.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is BindingAlarmReceiver {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === BindingAlarmReceiver.__pulumiType;
    }

    /**
     * Policy group ID for binding receivers.
     */
    public readonly groupId!: pulumi.Output<number>;
    /**
     * A list of receivers(will overwrite the configuration of the server or other resources). Each element contains the
     * following attributes:
     */
    public readonly receivers!: pulumi.Output<outputs.Monitor.BindingAlarmReceiverReceivers | undefined>;

    /**
     * Create a BindingAlarmReceiver resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: BindingAlarmReceiverArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: BindingAlarmReceiverArgs | BindingAlarmReceiverState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as BindingAlarmReceiverState | undefined;
            resourceInputs["groupId"] = state ? state.groupId : undefined;
            resourceInputs["receivers"] = state ? state.receivers : undefined;
        } else {
            const args = argsOrState as BindingAlarmReceiverArgs | undefined;
            if ((!args || args.groupId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'groupId'");
            }
            resourceInputs["groupId"] = args ? args.groupId : undefined;
            resourceInputs["receivers"] = args ? args.receivers : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(BindingAlarmReceiver.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering BindingAlarmReceiver resources.
 */
export interface BindingAlarmReceiverState {
    /**
     * Policy group ID for binding receivers.
     */
    groupId?: pulumi.Input<number>;
    /**
     * A list of receivers(will overwrite the configuration of the server or other resources). Each element contains the
     * following attributes:
     */
    receivers?: pulumi.Input<inputs.Monitor.BindingAlarmReceiverReceivers>;
}

/**
 * The set of arguments for constructing a BindingAlarmReceiver resource.
 */
export interface BindingAlarmReceiverArgs {
    /**
     * Policy group ID for binding receivers.
     */
    groupId: pulumi.Input<number>;
    /**
     * A list of receivers(will overwrite the configuration of the server or other resources). Each element contains the
     * following attributes:
     */
    receivers?: pulumi.Input<inputs.Monitor.BindingAlarmReceiverReceivers>;
}
