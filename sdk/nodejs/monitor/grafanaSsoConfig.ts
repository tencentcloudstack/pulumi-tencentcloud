// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a monitor grafanaSsoConfig
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const grafanaSsoConfig = new tencentcloud.monitor.GrafanaSsoConfig("grafana_sso_config", {
 *     instanceId: "grafana-dp2hnnfa",
 *     enableSso: false,
 * });
 * ```
 *
 * ## Import
 *
 * monitor grafana_sso_config can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Monitor/grafanaSsoConfig:GrafanaSsoConfig grafana_sso_config instance_id
 * ```
 */
export class GrafanaSsoConfig extends pulumi.CustomResource {
    /**
     * Get an existing GrafanaSsoConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: GrafanaSsoConfigState, opts?: pulumi.CustomResourceOptions): GrafanaSsoConfig {
        return new GrafanaSsoConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Monitor/grafanaSsoConfig:GrafanaSsoConfig';

    /**
     * Returns true if the given object is an instance of GrafanaSsoConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is GrafanaSsoConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === GrafanaSsoConfig.__pulumiType;
    }

    /**
     * Whether to enable SSO: `true` for enabling; `false` for disabling.
     */
    declare public readonly enableSso: pulumi.Output<boolean>;
    /**
     * Grafana instance ID.
     */
    declare public readonly instanceId: pulumi.Output<string>;

    /**
     * Create a GrafanaSsoConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: GrafanaSsoConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: GrafanaSsoConfigArgs | GrafanaSsoConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as GrafanaSsoConfigState | undefined;
            resourceInputs["enableSso"] = state?.enableSso;
            resourceInputs["instanceId"] = state?.instanceId;
        } else {
            const args = argsOrState as GrafanaSsoConfigArgs | undefined;
            if (args?.enableSso === undefined && !opts.urn) {
                throw new Error("Missing required property 'enableSso'");
            }
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["enableSso"] = args?.enableSso;
            resourceInputs["instanceId"] = args?.instanceId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(GrafanaSsoConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering GrafanaSsoConfig resources.
 */
export interface GrafanaSsoConfigState {
    /**
     * Whether to enable SSO: `true` for enabling; `false` for disabling.
     */
    enableSso?: pulumi.Input<boolean>;
    /**
     * Grafana instance ID.
     */
    instanceId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a GrafanaSsoConfig resource.
 */
export interface GrafanaSsoConfigArgs {
    /**
     * Whether to enable SSO: `true` for enabling; `false` for disabling.
     */
    enableSso: pulumi.Input<boolean>;
    /**
     * Grafana instance ID.
     */
    instanceId: pulumi.Input<string>;
}
