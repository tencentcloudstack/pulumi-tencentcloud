// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a tke tmpRecordRule
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as pulumi from "@tencentcloud_iac/pulumi";
 * import * as tencentcloud from "@pulumi/tencentcloud";
 *
 * const config = new pulumi.Config();
 * const defaultInstanceType = config.get("defaultInstanceType") || "SA1.MEDIUM2";
 * const availabilityZoneFirst = config.get("availabilityZoneFirst") || "ap-guangzhou-3";
 * const availabilityZoneSecond = config.get("availabilityZoneSecond") || "ap-guangzhou-4";
 * const exampleClusterCidr = config.get("exampleClusterCidr") || "10.31.0.0/16";
 * const vpcOne = tencentcloud.Vpc.getSubnets({
 *     isDefault: true,
 *     availabilityZone: availabilityZoneFirst,
 * });
 * const firstVpcId = vpcOne.then(vpcOne => vpcOne.instanceLists?[0]?.vpcId);
 * const firstSubnetId = vpcOne.then(vpcOne => vpcOne.instanceLists?[0]?.subnetId);
 * const vpcTwo = tencentcloud.Vpc.getSubnets({
 *     isDefault: true,
 *     availabilityZone: availabilityZoneSecond,
 * });
 * const secondVpcId = vpcTwo.then(vpcTwo => vpcTwo.instanceLists?[0]?.vpcId);
 * const secondSubnetId = vpcTwo.then(vpcTwo => vpcTwo.instanceLists?[0]?.subnetId);
 * const sg = new tencentcloud.security.Group("sg", {});
 * const sgId = sg.id;
 * const default = tencentcloud.Images.getInstance({
 *     imageTypes: ["PUBLIC_IMAGE"],
 *     imageNameRegex: "Final",
 * });
 * const imageId = _default.then(_default => _default.imageId);
 * const sgRule = new tencentcloud.security.GroupLiteRule("sgRule", {
 *     securityGroupId: sg.id,
 *     ingresses: [
 *         "ACCEPT#10.0.0.0/16#ALL#ALL",
 *         "ACCEPT#172.16.0.0/22#ALL#ALL",
 *         "DROP#0.0.0.0/0#ALL#ALL",
 *     ],
 *     egresses: ["ACCEPT#172.16.0.0/22#ALL#ALL"],
 * });
 * const example = new tencentcloud.kubernetes.Cluster("example", {
 *     vpcId: firstVpcId,
 *     clusterCidr: exampleClusterCidr,
 *     clusterMaxPodNum: 32,
 *     clusterName: "tf_example_cluster",
 *     clusterDesc: "example for tke cluster",
 *     clusterMaxServiceNum: 32,
 *     clusterInternet: false,
 *     clusterInternetSecurityGroup: sgId,
 *     clusterVersion: "1.22.5",
 *     clusterDeployType: "MANAGED_CLUSTER",
 *     workerConfigs: [
 *         {
 *             count: 1,
 *             availabilityZone: availabilityZoneFirst,
 *             instanceType: defaultInstanceType,
 *             systemDiskType: "CLOUD_SSD",
 *             systemDiskSize: 60,
 *             internetChargeType: "TRAFFIC_POSTPAID_BY_HOUR",
 *             internetMaxBandwidthOut: 100,
 *             publicIpAssigned: true,
 *             subnetId: firstSubnetId,
 *             imgId: imageId,
 *             dataDisks: [{
 *                 diskType: "CLOUD_PREMIUM",
 *                 diskSize: 50,
 *             }],
 *             enhancedSecurityService: false,
 *             enhancedMonitorService: false,
 *             userData: "dGVzdA==",
 *             password: "ZZXXccvv1212",
 *         },
 *         {
 *             count: 1,
 *             availabilityZone: availabilityZoneSecond,
 *             instanceType: defaultInstanceType,
 *             systemDiskType: "CLOUD_SSD",
 *             systemDiskSize: 60,
 *             internetChargeType: "TRAFFIC_POSTPAID_BY_HOUR",
 *             internetMaxBandwidthOut: 100,
 *             publicIpAssigned: true,
 *             subnetId: secondSubnetId,
 *             dataDisks: [{
 *                 diskType: "CLOUD_PREMIUM",
 *                 diskSize: 50,
 *             }],
 *             enhancedSecurityService: false,
 *             enhancedMonitorService: false,
 *             userData: "dGVzdA==",
 *             camRoleName: "CVM_QcsRole",
 *             password: "ZZXXccvv1212",
 *         },
 *     ],
 *     labels: {
 *         test1: "test1",
 *         test2: "test2",
 *     },
 * });
 * const zone = config.get("zone") || "ap-guangzhou";
 * const clusterType = config.get("clusterType") || "tke";
 * const fooTmpInstance = new tencentcloud.monitor.TmpInstance("fooTmpInstance", {
 *     instanceName: "tf-tmp-instance",
 *     vpcId: firstVpcId,
 *     subnetId: firstSubnetId,
 *     dataRetentionTime: 30,
 *     zone: availabilityZoneSecond,
 *     tags: {
 *         createdBy: "terraform",
 *     },
 * });
 * // tmp tke bind
 * const fooTmpTkeClusterAgent = new tencentcloud.monitor.TmpTkeClusterAgent("fooTmpTkeClusterAgent", {
 *     instanceId: fooTmpInstance.id,
 *     agents: {
 *         region: zone,
 *         clusterType: clusterType,
 *         clusterId: example.id,
 *         enableExternal: false,
 *     },
 * });
 * // create record rule
 * const fooTmpTkeRecordRuleYaml = new tencentcloud.monitor.TmpTkeRecordRuleYaml("fooTmpTkeRecordRuleYaml", {
 *     instanceId: fooTmpInstance.id,
 *     content: `apiVersion: monitoring.coreos.com/v1
 * kind: PrometheusRule
 * metadata:
 *   name: example-record
 * spec:
 *   groups:
 *     - name: kube-apiserver.rules
 *       rules:
 *         - expr: sum(metrics_test)
 *           labels:
 *             verb: read
 *           record: 'apiserver_request:burnrate1d'
 * `,
 * }, {
 *     dependsOn: [fooTmpTkeClusterAgent],
 * });
 * ```
 */
export class TmpTkeRecordRuleYaml extends pulumi.CustomResource {
    /**
     * Get an existing TmpTkeRecordRuleYaml resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: TmpTkeRecordRuleYamlState, opts?: pulumi.CustomResourceOptions): TmpTkeRecordRuleYaml {
        return new TmpTkeRecordRuleYaml(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Monitor/tmpTkeRecordRuleYaml:TmpTkeRecordRuleYaml';

    /**
     * Returns true if the given object is an instance of TmpTkeRecordRuleYaml.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is TmpTkeRecordRuleYaml {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TmpTkeRecordRuleYaml.__pulumiType;
    }

    /**
     * An ID identify the cluster, like cls-xxxxxx.
     */
    public /*out*/ readonly clusterId!: pulumi.Output<string>;
    /**
     * Contents of record rules in yaml format.
     */
    public readonly content!: pulumi.Output<string>;
    /**
     * Instance Id.
     */
    public readonly instanceId!: pulumi.Output<string>;
    /**
     * Name of the instance.
     */
    public /*out*/ readonly name!: pulumi.Output<string>;
    /**
     * Used for the argument, if the configuration comes to the template, the template id.
     */
    public /*out*/ readonly templateId!: pulumi.Output<string>;
    /**
     * Last modified time of record rule.
     */
    public /*out*/ readonly updateTime!: pulumi.Output<string>;

    /**
     * Create a TmpTkeRecordRuleYaml resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: TmpTkeRecordRuleYamlArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: TmpTkeRecordRuleYamlArgs | TmpTkeRecordRuleYamlState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as TmpTkeRecordRuleYamlState | undefined;
            resourceInputs["clusterId"] = state ? state.clusterId : undefined;
            resourceInputs["content"] = state ? state.content : undefined;
            resourceInputs["instanceId"] = state ? state.instanceId : undefined;
            resourceInputs["name"] = state ? state.name : undefined;
            resourceInputs["templateId"] = state ? state.templateId : undefined;
            resourceInputs["updateTime"] = state ? state.updateTime : undefined;
        } else {
            const args = argsOrState as TmpTkeRecordRuleYamlArgs | undefined;
            if ((!args || args.content === undefined) && !opts.urn) {
                throw new Error("Missing required property 'content'");
            }
            if ((!args || args.instanceId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["content"] = args ? args.content : undefined;
            resourceInputs["instanceId"] = args ? args.instanceId : undefined;
            resourceInputs["clusterId"] = undefined /*out*/;
            resourceInputs["name"] = undefined /*out*/;
            resourceInputs["templateId"] = undefined /*out*/;
            resourceInputs["updateTime"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(TmpTkeRecordRuleYaml.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering TmpTkeRecordRuleYaml resources.
 */
export interface TmpTkeRecordRuleYamlState {
    /**
     * An ID identify the cluster, like cls-xxxxxx.
     */
    clusterId?: pulumi.Input<string>;
    /**
     * Contents of record rules in yaml format.
     */
    content?: pulumi.Input<string>;
    /**
     * Instance Id.
     */
    instanceId?: pulumi.Input<string>;
    /**
     * Name of the instance.
     */
    name?: pulumi.Input<string>;
    /**
     * Used for the argument, if the configuration comes to the template, the template id.
     */
    templateId?: pulumi.Input<string>;
    /**
     * Last modified time of record rule.
     */
    updateTime?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a TmpTkeRecordRuleYaml resource.
 */
export interface TmpTkeRecordRuleYamlArgs {
    /**
     * Contents of record rules in yaml format.
     */
    content: pulumi.Input<string>;
    /**
     * Instance Id.
     */
    instanceId: pulumi.Input<string>;
}
