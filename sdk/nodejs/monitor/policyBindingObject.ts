// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource for bind objects to a alarm policy resource.
 *
 * ## Example Usage
 *
 * <!--Start PulumiCodeChooser -->
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@pulumi/tencentcloud";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const instances = tencentcloud.Instances.getInstance({});
 * const policy = new tencentcloud.monitor.AlarmPolicy("policy", {
 *     policyName: "hello",
 *     monitorType: "MT_QCE",
 *     enable: 1,
 *     projectId: 1244035,
 *     namespace: "cvm_device",
 *     conditions: {
 *         isUnionRule: 1,
 *         rules: [{
 *             metricName: "CpuUsage",
 *             period: 60,
 *             operator: "ge",
 *             value: "89.9",
 *             continuePeriod: 1,
 *             noticeFrequency: 3600,
 *             isPowerNotice: 0,
 *         }],
 *     },
 *     eventConditions: [
 *         {
 *             metricName: "ping_unreachable",
 *         },
 *         {
 *             metricName: "guest_reboot",
 *         },
 *     ],
 *     noticeIds: ["notice-l9ziyxw6"],
 *     triggerTasks: [{
 *         type: "AS",
 *         taskConfig: "{\"Region\":\"ap-guangzhou\",\"Group\":\"asg-0z312312x\",\"Policy\":\"asp-ganig28\"}",
 *     }],
 * });
 * //for cvm
 * const binding = new tencentcloud.monitor.PolicyBindingObject("binding", {
 *     policyId: policy.id,
 *     dimensions: [{
 *         dimensionsJson: instances.then(instances => `{"unInstanceId":"${instances.instanceLists?.[0]?.instanceId}"}`),
 *     }],
 * });
 * ```
 * <!--End PulumiCodeChooser -->
 *
 * ## Import
 *
 * Monitor Policy Binding Object can be imported, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Monitor/policyBindingObject:PolicyBindingObject binding policyId
 * ```
 */
export class PolicyBindingObject extends pulumi.CustomResource {
    /**
     * Get an existing PolicyBindingObject resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: PolicyBindingObjectState, opts?: pulumi.CustomResourceOptions): PolicyBindingObject {
        return new PolicyBindingObject(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Monitor/policyBindingObject:PolicyBindingObject';

    /**
     * Returns true if the given object is an instance of PolicyBindingObject.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is PolicyBindingObject {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === PolicyBindingObject.__pulumiType;
    }

    /**
     * A list objects. Each element contains the following attributes:
     */
    public readonly dimensions!: pulumi.Output<outputs.Monitor.PolicyBindingObjectDimension[]>;
    /**
     * Alarm policy ID for binding objects.
     */
    public readonly policyId!: pulumi.Output<string>;

    /**
     * Create a PolicyBindingObject resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: PolicyBindingObjectArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: PolicyBindingObjectArgs | PolicyBindingObjectState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as PolicyBindingObjectState | undefined;
            resourceInputs["dimensions"] = state ? state.dimensions : undefined;
            resourceInputs["policyId"] = state ? state.policyId : undefined;
        } else {
            const args = argsOrState as PolicyBindingObjectArgs | undefined;
            if ((!args || args.dimensions === undefined) && !opts.urn) {
                throw new Error("Missing required property 'dimensions'");
            }
            if ((!args || args.policyId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'policyId'");
            }
            resourceInputs["dimensions"] = args ? args.dimensions : undefined;
            resourceInputs["policyId"] = args ? args.policyId : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(PolicyBindingObject.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering PolicyBindingObject resources.
 */
export interface PolicyBindingObjectState {
    /**
     * A list objects. Each element contains the following attributes:
     */
    dimensions?: pulumi.Input<pulumi.Input<inputs.Monitor.PolicyBindingObjectDimension>[]>;
    /**
     * Alarm policy ID for binding objects.
     */
    policyId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a PolicyBindingObject resource.
 */
export interface PolicyBindingObjectArgs {
    /**
     * A list objects. Each element contains the following attributes:
     */
    dimensions: pulumi.Input<pulumi.Input<inputs.Monitor.PolicyBindingObjectDimension>[]>;
    /**
     * Alarm policy ID for binding objects.
     */
    policyId: pulumi.Input<string>;
}
