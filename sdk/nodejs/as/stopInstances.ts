// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a as stopInstances
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as pulumi from "@tencentcloud_iac/pulumi";
 * import * as tencentcloud from "@pulumi/tencentcloud";
 *
 * const zones = tencentcloud.Availability.getZonesByProduct({
 *     product: "as",
 * });
 * const image = tencentcloud.Images.getInstance({
 *     imageTypes: ["PUBLIC_IMAGE"],
 *     osName: "TencentOS Server 3.2 (Final)",
 * });
 * const instanceTypes = zones.then(zones => tencentcloud.Instance.getTypes({
 *     filters: [
 *         {
 *             name: "zone",
 *             values: [zones.zones?[0]?.name],
 *         },
 *         {
 *             name: "instance-family",
 *             values: ["S5"],
 *         },
 *     ],
 *     cpuCoreCount: 2,
 *     excludeSoldOut: true,
 * }));
 * const vpc = new tencentcloud.vpc.Instance("vpc", {cidrBlock: "10.0.0.0/16"});
 * const subnet = new tencentcloud.subnet.Instance("subnet", {
 *     vpcId: vpc.id,
 *     cidrBlock: "10.0.0.0/16",
 *     availabilityZone: zones.then(zones => zones.zones?[0]?.name),
 * });
 * const exampleScalingConfig = new tencentcloud.as.ScalingConfig("exampleScalingConfig", {
 *     configurationName: "tf-example",
 *     imageId: image.then(image => image.images?[0]?.imageId),
 *     instanceTypes: [
 *         "SA1.SMALL1",
 *         "SA2.SMALL1",
 *         "SA2.SMALL2",
 *         "SA2.SMALL4",
 *     ],
 *     instanceNameSettings: {
 *         instanceName: "test-ins-name",
 *     },
 * });
 * const exampleScalingGroup = new tencentcloud.as.ScalingGroup("exampleScalingGroup", {
 *     scalingGroupName: "tf-example",
 *     configurationId: exampleScalingConfig.id,
 *     maxSize: 1,
 *     minSize: 0,
 *     vpcId: vpc.id,
 *     subnetIds: [subnet.id],
 * });
 * const exampleInstance = new tencentcloud.instance.Instance("exampleInstance", {
 *     instanceName: "tf_example",
 *     availabilityZone: zones.then(zones => zones.zones?[0]?.name),
 *     imageId: image.then(image => image.images?[0]?.imageId),
 *     instanceType: instanceTypes.then(instanceTypes => instanceTypes.instanceTypes?[0]?.instanceType),
 *     systemDiskType: "CLOUD_PREMIUM",
 *     systemDiskSize: 50,
 *     hostname: "user",
 *     projectId: 0,
 *     vpcId: vpc.id,
 *     subnetId: subnet.id,
 * });
 * // Attachment Instance
 * const attachment = new tencentcloud.as.Attachment("attachment", {
 *     scalingGroupId: exampleScalingGroup.id,
 *     instanceIds: [exampleInstance.id],
 * });
 * const stopInstances = new tencentcloud.as.StopInstances("stopInstances", {
 *     autoScalingGroupId: exampleScalingGroup.id,
 *     instanceIds: attachment.instanceIds,
 *     stoppedMode: "STOP_CHARGING",
 * });
 * ```
 */
export class StopInstances extends pulumi.CustomResource {
    /**
     * Get an existing StopInstances resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: StopInstancesState, opts?: pulumi.CustomResourceOptions): StopInstances {
        return new StopInstances(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:As/stopInstances:StopInstances';

    /**
     * Returns true if the given object is an instance of StopInstances.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is StopInstances {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === StopInstances.__pulumiType;
    }

    /**
     * Launch configuration ID.
     */
    public readonly autoScalingGroupId!: pulumi.Output<string>;
    /**
     * List of cvm instances to stop.
     */
    public readonly instanceIds!: pulumi.Output<string[]>;
    /**
     * Billing method of a pay-as-you-go instance after shutdown. Available values: `KEEP_CHARGING`,`STOP_CHARGING`. Default `KEEP_CHARGING`.
     */
    public readonly stoppedMode!: pulumi.Output<string | undefined>;

    /**
     * Create a StopInstances resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: StopInstancesArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: StopInstancesArgs | StopInstancesState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as StopInstancesState | undefined;
            resourceInputs["autoScalingGroupId"] = state ? state.autoScalingGroupId : undefined;
            resourceInputs["instanceIds"] = state ? state.instanceIds : undefined;
            resourceInputs["stoppedMode"] = state ? state.stoppedMode : undefined;
        } else {
            const args = argsOrState as StopInstancesArgs | undefined;
            if ((!args || args.autoScalingGroupId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'autoScalingGroupId'");
            }
            if ((!args || args.instanceIds === undefined) && !opts.urn) {
                throw new Error("Missing required property 'instanceIds'");
            }
            resourceInputs["autoScalingGroupId"] = args ? args.autoScalingGroupId : undefined;
            resourceInputs["instanceIds"] = args ? args.instanceIds : undefined;
            resourceInputs["stoppedMode"] = args ? args.stoppedMode : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(StopInstances.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering StopInstances resources.
 */
export interface StopInstancesState {
    /**
     * Launch configuration ID.
     */
    autoScalingGroupId?: pulumi.Input<string>;
    /**
     * List of cvm instances to stop.
     */
    instanceIds?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Billing method of a pay-as-you-go instance after shutdown. Available values: `KEEP_CHARGING`,`STOP_CHARGING`. Default `KEEP_CHARGING`.
     */
    stoppedMode?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a StopInstances resource.
 */
export interface StopInstancesArgs {
    /**
     * Launch configuration ID.
     */
    autoScalingGroupId: pulumi.Input<string>;
    /**
     * List of cvm instances to stop.
     */
    instanceIds: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Billing method of a pay-as-you-go instance after shutdown. Available values: `KEEP_CHARGING`,`STOP_CHARGING`. Default `KEEP_CHARGING`.
     */
    stoppedMode?: pulumi.Input<string>;
}
