// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a as load balancer
 *
 * > **NOTE:** This resource must exclusive in one auto scaling group, do not declare additional rule resources of this auto scaling group elsewhere.
 *
 * > **NOTE:** If the `autoScalingGroupId` field of this resource comes from the `tencentcloud.As.ScalingGroup` resource, then the `forwardBalancerIds` field of the `tencentcloud.As.ScalingGroup` resource cannot be set simultaneously with this resource, which may result in conflicts
 *
 * > **NOTE:** `forwardLoadBalancers` List of application type load balancers, with a maximum of 100 bound application type load balancers for each scaling group.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const config = new pulumi.Config();
 * const availabilityZone = config.get("availabilityZone") || "ap-guangzhou-6";
 * // create vpc
 * const vpc = new tencentcloud.vpc.Instance("vpc", {
 *     cidrBlock: "10.0.0.0/16",
 *     name: "vpc",
 * });
 * // create subnet
 * const subnet = new tencentcloud.subnet.Instance("subnet", {
 *     vpcId: vpc.id,
 *     availabilityZone: availabilityZone,
 *     name: "subnet",
 *     cidrBlock: "10.0.1.0/24",
 *     isMulticast: false,
 * });
 * const example = new tencentcloud.as.ScalingConfig("example", {
 *     configurationName: "tf-example",
 *     imageId: "img-eb30mz89",
 *     instanceTypes: ["S6.MEDIUM4"],
 *     instanceNameSettings: {
 *         instanceName: "demo-ins-name",
 *     },
 * });
 * const exampleScalingGroup = new tencentcloud.as.ScalingGroup("example", {
 *     scalingGroupName: "tf-example",
 *     configurationId: example.id,
 *     maxSize: 1,
 *     minSize: 0,
 *     vpcId: vpc.id,
 *     subnetIds: [subnet.id],
 * });
 * const exampleInstance = new tencentcloud.clb.Instance("example", {
 *     networkType: "INTERNAL",
 *     clbName: "tf-example",
 *     vpcId: vpc.id,
 *     subnetId: subnet.id,
 *     tags: {
 *         createBy: "Terraform",
 *     },
 * });
 * const exampleListener = new tencentcloud.clb.Listener("example", {
 *     clbId: exampleInstance.id,
 *     listenerName: "tf-example",
 *     port: 80,
 *     protocol: "HTTP",
 * });
 * const exampleListenerRule = new tencentcloud.clb.ListenerRule("example", {
 *     listenerId: exampleListener.listenerId,
 *     clbId: exampleInstance.id,
 *     domain: "foo.net",
 *     url: "/bar",
 * });
 * const exampleLoadBalancer = new tencentcloud.as.LoadBalancer("example", {
 *     autoScalingGroupId: exampleScalingGroup.id,
 *     forwardLoadBalancers: [{
 *         loadBalancerId: exampleInstance.id,
 *         listenerId: exampleListener.listenerId,
 *         locationId: exampleListenerRule.ruleId,
 *         targetAttributes: [{
 *             port: 8080,
 *             weight: 20,
 *         }],
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * as load balancer can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:As/loadBalancer:LoadBalancer example asg-bpp4uol2
 * ```
 */
export class LoadBalancer extends pulumi.CustomResource {
    /**
     * Get an existing LoadBalancer resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: LoadBalancerState, opts?: pulumi.CustomResourceOptions): LoadBalancer {
        return new LoadBalancer(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:As/loadBalancer:LoadBalancer';

    /**
     * Returns true if the given object is an instance of LoadBalancer.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is LoadBalancer {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === LoadBalancer.__pulumiType;
    }

    /**
     * ID of a scaling group.
     */
    declare public readonly autoScalingGroupId: pulumi.Output<string>;
    /**
     * List of application load balancers. The maximum number of application-type load balancers bound to each scaling group is 100.
     */
    declare public readonly forwardLoadBalancers: pulumi.Output<outputs.As.LoadBalancerForwardLoadBalancer[] | undefined>;

    /**
     * Create a LoadBalancer resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: LoadBalancerArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: LoadBalancerArgs | LoadBalancerState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as LoadBalancerState | undefined;
            resourceInputs["autoScalingGroupId"] = state?.autoScalingGroupId;
            resourceInputs["forwardLoadBalancers"] = state?.forwardLoadBalancers;
        } else {
            const args = argsOrState as LoadBalancerArgs | undefined;
            if (args?.autoScalingGroupId === undefined && !opts.urn) {
                throw new Error("Missing required property 'autoScalingGroupId'");
            }
            resourceInputs["autoScalingGroupId"] = args?.autoScalingGroupId;
            resourceInputs["forwardLoadBalancers"] = args?.forwardLoadBalancers;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(LoadBalancer.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering LoadBalancer resources.
 */
export interface LoadBalancerState {
    /**
     * ID of a scaling group.
     */
    autoScalingGroupId?: pulumi.Input<string>;
    /**
     * List of application load balancers. The maximum number of application-type load balancers bound to each scaling group is 100.
     */
    forwardLoadBalancers?: pulumi.Input<pulumi.Input<inputs.As.LoadBalancerForwardLoadBalancer>[]>;
}

/**
 * The set of arguments for constructing a LoadBalancer resource.
 */
export interface LoadBalancerArgs {
    /**
     * ID of a scaling group.
     */
    autoScalingGroupId: pulumi.Input<string>;
    /**
     * List of application load balancers. The maximum number of application-type load balancers bound to each scaling group is 100.
     */
    forwardLoadBalancers?: pulumi.Input<pulumi.Input<inputs.As.LoadBalancerForwardLoadBalancer>[]>;
}
