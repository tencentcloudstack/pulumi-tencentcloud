// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a as scaleInInstances
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const zones = tencentcloud.Availability.getZonesByProduct({
 *     product: "as",
 * });
 * const image = tencentcloud.Images.getInstance({
 *     imageTypes: ["PUBLIC_IMAGE"],
 *     osName: "TencentOS Server 3.2 (Final)",
 * });
 * const vpc = new tencentcloud.vpc.Instance("vpc", {
 *     name: "vpc-example",
 *     cidrBlock: "10.0.0.0/16",
 * });
 * const subnet = new tencentcloud.subnet.Instance("subnet", {
 *     vpcId: vpc.id,
 *     name: "subnet-example",
 *     cidrBlock: "10.0.0.0/16",
 *     availabilityZone: zones.then(zones => zones.zones?.[0]?.name),
 * });
 * const example = new tencentcloud.as.ScalingConfig("example", {
 *     configurationName: "tf-example",
 *     imageId: image.then(image => image.images?.[0]?.imageId),
 *     instanceTypes: [
 *         "SA1.SMALL1",
 *         "SA2.SMALL1",
 *         "SA2.SMALL2",
 *         "SA2.SMALL4",
 *     ],
 *     instanceNameSettings: {
 *         instanceName: "test-ins-name",
 *     },
 * });
 * const exampleScalingGroup = new tencentcloud.as.ScalingGroup("example", {
 *     scalingGroupName: "tf-example",
 *     configurationId: example.id,
 *     maxSize: 4,
 *     minSize: 1,
 *     desiredCapacity: 2,
 *     vpcId: vpc.id,
 *     subnetIds: [subnet.id],
 * });
 * const scaleInInstances = new tencentcloud.as.ScaleInInstances("scale_in_instances", {
 *     autoScalingGroupId: exampleScalingGroup.id,
 *     scaleInNumber: 1,
 * });
 * ```
 *
 * ## Import
 *
 * as scale_in_instances can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:As/scaleInInstances:ScaleInInstances scale_in_instances scale_in_instances_id
 * ```
 */
export class ScaleInInstances extends pulumi.CustomResource {
    /**
     * Get an existing ScaleInInstances resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ScaleInInstancesState, opts?: pulumi.CustomResourceOptions): ScaleInInstances {
        return new ScaleInInstances(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:As/scaleInInstances:ScaleInInstances';

    /**
     * Returns true if the given object is an instance of ScaleInInstances.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ScaleInInstances {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ScaleInInstances.__pulumiType;
    }

    /**
     * Scaling group ID.
     */
    declare public readonly autoScalingGroupId: pulumi.Output<string>;
    /**
     * Number of instances to be reduced.
     */
    declare public readonly scaleInNumber: pulumi.Output<number>;

    /**
     * Create a ScaleInInstances resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ScaleInInstancesArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ScaleInInstancesArgs | ScaleInInstancesState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ScaleInInstancesState | undefined;
            resourceInputs["autoScalingGroupId"] = state?.autoScalingGroupId;
            resourceInputs["scaleInNumber"] = state?.scaleInNumber;
        } else {
            const args = argsOrState as ScaleInInstancesArgs | undefined;
            if (args?.autoScalingGroupId === undefined && !opts.urn) {
                throw new Error("Missing required property 'autoScalingGroupId'");
            }
            if (args?.scaleInNumber === undefined && !opts.urn) {
                throw new Error("Missing required property 'scaleInNumber'");
            }
            resourceInputs["autoScalingGroupId"] = args?.autoScalingGroupId;
            resourceInputs["scaleInNumber"] = args?.scaleInNumber;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ScaleInInstances.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ScaleInInstances resources.
 */
export interface ScaleInInstancesState {
    /**
     * Scaling group ID.
     */
    autoScalingGroupId?: pulumi.Input<string>;
    /**
     * Number of instances to be reduced.
     */
    scaleInNumber?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a ScaleInInstances resource.
 */
export interface ScaleInInstancesArgs {
    /**
     * Scaling group ID.
     */
    autoScalingGroupId: pulumi.Input<string>;
    /**
     * Number of instances to be reduced.
     */
    scaleInNumber: pulumi.Input<number>;
}
