// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a TKE kubernetes user permissions
 *
 * > **NOTE:** This resource must exclusive in one target Uin, do not declare additional permissions resources of this target Uin elsewhere.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.kubernetes.UserPermissions("example", {
 *     targetUin: "100056451191",
 *     permissions: [
 *         {
 *             clusterId: "cls-62ch3v24",
 *             roleName: "tke:admin",
 *             roleType: "cluster",
 *             isCustom: false,
 *         },
 *         {
 *             clusterId: "cls-62ch3v24",
 *             roleName: "tke:admin",
 *             roleType: "namespace",
 *             isCustom: false,
 *             namespace: "default",
 *         },
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * TKE kubernetes user permissions can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Kubernetes/userPermissions:UserPermissions example 100056451191
 * ```
 */
export class UserPermissions extends pulumi.CustomResource {
    /**
     * Get an existing UserPermissions resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: UserPermissionsState, opts?: pulumi.CustomResourceOptions): UserPermissions {
        return new UserPermissions(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Kubernetes/userPermissions:UserPermissions';

    /**
     * Returns true if the given object is an instance of UserPermissions.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is UserPermissions {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === UserPermissions.__pulumiType;
    }

    /**
     * Complete list of permissions that the user should ultimately have. Uses declarative semantics, the passed list represents all permissions the user should ultimately have, the system will automatically calculate differences and perform necessary create/delete operations. When empty or not provided, all permissions for this user will be cleared. Maximum support for 100 permission items.
     */
    declare public readonly permissions: pulumi.Output<outputs.Kubernetes.UserPermissionsPermission[] | undefined>;
    /**
     * Unique identifier of the user to be authorized (supports sub-account UIN and role UIN).
     */
    declare public readonly targetUin: pulumi.Output<string>;

    /**
     * Create a UserPermissions resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: UserPermissionsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: UserPermissionsArgs | UserPermissionsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as UserPermissionsState | undefined;
            resourceInputs["permissions"] = state?.permissions;
            resourceInputs["targetUin"] = state?.targetUin;
        } else {
            const args = argsOrState as UserPermissionsArgs | undefined;
            if (args?.targetUin === undefined && !opts.urn) {
                throw new Error("Missing required property 'targetUin'");
            }
            resourceInputs["permissions"] = args?.permissions;
            resourceInputs["targetUin"] = args?.targetUin;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(UserPermissions.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering UserPermissions resources.
 */
export interface UserPermissionsState {
    /**
     * Complete list of permissions that the user should ultimately have. Uses declarative semantics, the passed list represents all permissions the user should ultimately have, the system will automatically calculate differences and perform necessary create/delete operations. When empty or not provided, all permissions for this user will be cleared. Maximum support for 100 permission items.
     */
    permissions?: pulumi.Input<pulumi.Input<inputs.Kubernetes.UserPermissionsPermission>[] | undefined>;
    /**
     * Unique identifier of the user to be authorized (supports sub-account UIN and role UIN).
     */
    targetUin?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a UserPermissions resource.
 */
export interface UserPermissionsArgs {
    /**
     * Complete list of permissions that the user should ultimately have. Uses declarative semantics, the passed list represents all permissions the user should ultimately have, the system will automatically calculate differences and perform necessary create/delete operations. When empty or not provided, all permissions for this user will be cleared. Maximum support for 100 permission items.
     */
    permissions?: pulumi.Input<pulumi.Input<inputs.Kubernetes.UserPermissionsPermission>[] | undefined>;
    /**
     * Unique identifier of the user to be authorized (supports sub-account UIN and role UIN).
     */
    targetUin: pulumi.Input<string>;
}
