// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a TCSS refresh task operation
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.tcss.RefreshTaskOperation("example", {});
 * ```
 *
 * ### Or
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.tcss.RefreshTaskOperation("example", {
 *     clusterIds: ["cls-fdy7hm1q"],
 *     isSyncListOnly: false,
 * });
 * ```
 */
export class RefreshTaskOperation extends pulumi.CustomResource {
    /**
     * Get an existing RefreshTaskOperation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RefreshTaskOperationState, opts?: pulumi.CustomResourceOptions): RefreshTaskOperation {
        return new RefreshTaskOperation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Tcss/refreshTaskOperation:RefreshTaskOperation';

    /**
     * Returns true if the given object is an instance of RefreshTaskOperation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RefreshTaskOperation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RefreshTaskOperation.__pulumiType;
    }

    /**
     * Cluster Id list.
     */
    declare public readonly clusterIds: pulumi.Output<string[] | undefined>;
    /**
     * Whether to sync list only.
     */
    declare public readonly isSyncListOnly: pulumi.Output<boolean | undefined>;

    /**
     * Create a RefreshTaskOperation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: RefreshTaskOperationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RefreshTaskOperationArgs | RefreshTaskOperationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RefreshTaskOperationState | undefined;
            resourceInputs["clusterIds"] = state?.clusterIds;
            resourceInputs["isSyncListOnly"] = state?.isSyncListOnly;
        } else {
            const args = argsOrState as RefreshTaskOperationArgs | undefined;
            resourceInputs["clusterIds"] = args?.clusterIds;
            resourceInputs["isSyncListOnly"] = args?.isSyncListOnly;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RefreshTaskOperation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RefreshTaskOperation resources.
 */
export interface RefreshTaskOperationState {
    /**
     * Cluster Id list.
     */
    clusterIds?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Whether to sync list only.
     */
    isSyncListOnly?: pulumi.Input<boolean>;
}

/**
 * The set of arguments for constructing a RefreshTaskOperation resource.
 */
export interface RefreshTaskOperationArgs {
    /**
     * Cluster Id list.
     */
    clusterIds?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Whether to sync list only.
     */
    isSyncListOnly?: pulumi.Input<boolean>;
}
