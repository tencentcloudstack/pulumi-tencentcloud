// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a dnspod domainLock
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const domainLock = new tencentcloud.dnspod.DomainLock("domain_lock", {
 *     domain: "dnspod.cn",
 *     lockDays: 30,
 * });
 * ```
 */
export class DomainLock extends pulumi.CustomResource {
    /**
     * Get an existing DomainLock resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: DomainLockState, opts?: pulumi.CustomResourceOptions): DomainLock {
        return new DomainLock(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Dnspod/domainLock:DomainLock';

    /**
     * Returns true if the given object is an instance of DomainLock.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is DomainLock {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === DomainLock.__pulumiType;
    }

    /**
     * Domain name.
     */
    declare public readonly domain: pulumi.Output<string>;
    /**
     * Domain unlock code, can be obtained through the ModifyDomainLock interface.
     */
    declare public /*out*/ readonly lockCode: pulumi.Output<string>;
    /**
     * The number of max days to lock the domain+ Old packages: D_FREE 30 days, D_PLUS 90 days, D_EXTRA 30 days, D_EXPERT 60 days, D_ULTRA 365 days+ New packages: DP_FREE 365 days, DP_PLUS 365 days, DP_EXTRA 365 days, DP_EXPERT 365 days, DP_ULTRA 365 days.
     */
    declare public readonly lockDays: pulumi.Output<number>;

    /**
     * Create a DomainLock resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: DomainLockArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: DomainLockArgs | DomainLockState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as DomainLockState | undefined;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["lockCode"] = state?.lockCode;
            resourceInputs["lockDays"] = state?.lockDays;
        } else {
            const args = argsOrState as DomainLockArgs | undefined;
            if (args?.domain === undefined && !opts.urn) {
                throw new Error("Missing required property 'domain'");
            }
            if (args?.lockDays === undefined && !opts.urn) {
                throw new Error("Missing required property 'lockDays'");
            }
            resourceInputs["domain"] = args?.domain;
            resourceInputs["lockDays"] = args?.lockDays;
            resourceInputs["lockCode"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(DomainLock.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering DomainLock resources.
 */
export interface DomainLockState {
    /**
     * Domain name.
     */
    domain?: pulumi.Input<string>;
    /**
     * Domain unlock code, can be obtained through the ModifyDomainLock interface.
     */
    lockCode?: pulumi.Input<string>;
    /**
     * The number of max days to lock the domain+ Old packages: D_FREE 30 days, D_PLUS 90 days, D_EXTRA 30 days, D_EXPERT 60 days, D_ULTRA 365 days+ New packages: DP_FREE 365 days, DP_PLUS 365 days, DP_EXTRA 365 days, DP_EXPERT 365 days, DP_ULTRA 365 days.
     */
    lockDays?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a DomainLock resource.
 */
export interface DomainLockArgs {
    /**
     * Domain name.
     */
    domain: pulumi.Input<string>;
    /**
     * The number of max days to lock the domain+ Old packages: D_FREE 30 days, D_PLUS 90 days, D_EXTRA 30 days, D_EXPERT 60 days, D_ULTRA 365 days+ New packages: DP_FREE 365 days, DP_PLUS 365 days, DP_EXTRA 365 days, DP_EXPERT 365 days, DP_ULTRA 365 days.
     */
    lockDays: pulumi.Input<number>;
}
