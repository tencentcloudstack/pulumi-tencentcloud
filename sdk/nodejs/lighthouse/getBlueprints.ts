// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a list of Lighthouse blueprints (images).
 *
 * Use this data source to query available blueprints for Lighthouse instances.
 *
 * ## Example Usage
 *
 * ### Query all blueprints:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const all = tencentcloud.lighthouse.getBlueprints({});
 * export const blueprints = all.then(all => all.blueprintSets);
 * ```
 *
 * ### Filter by platform type:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const linux = tencentcloud.lighthouse.getBlueprints({
 *     filters: [{
 *         name: "platform-type",
 *         values: ["LINUX_UNIX"],
 *     }],
 * });
 * ```
 *
 * ### Filter by blueprint type:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const appOs = tencentcloud.lighthouse.getBlueprints({
 *     filters: [{
 *         name: "blueprint-type",
 *         values: ["APP_OS"],
 *     }],
 * });
 * ```
 *
 * ### Query specific blueprints by ID:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const specific = tencentcloud.lighthouse.getBlueprints({
 *     blueprintIds: [
 *         "lhbp-xxx",
 *         "lhbp-yyy",
 *     ],
 * });
 * ```
 */
export function getBlueprints(args?: GetBlueprintsArgs, opts?: pulumi.InvokeOptions): Promise<GetBlueprintsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("tencentcloud:Lighthouse/getBlueprints:getBlueprints", {
        "blueprintIds": args.blueprintIds,
        "filters": args.filters,
        "resultOutputFile": args.resultOutputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getBlueprints.
 */
export interface GetBlueprintsArgs {
    /**
     * Blueprint ID list.
     */
    blueprintIds?: string[];
    /**
     * Filter list.
     * - `blueprint-id`: Filter by blueprint ID.
     * - `blueprint-type`: Filter by blueprint type. Values: `APP_OS`, `PURE_OS`, `DOCKER`, `PRIVATE`, `SHARED`.
     * - `platform-type`: Filter by platform type. Values: `LINUX_UNIX`, `WINDOWS`.
     * - `blueprint-name`: Filter by blueprint name.
     * - `blueprint-state`: Filter by blueprint state.
     * - `scene-id`: Filter by scene ID.
     *   NOTE: The upper limit of Filters per request is 10. The upper limit of Filter.Values is 100. Parameter does not support specifying both BlueprintIds and Filters.
     */
    filters?: inputs.Lighthouse.GetBlueprintsFilter[];
    /**
     * Used to save results.
     */
    resultOutputFile?: string;
}

/**
 * A collection of values returned by getBlueprints.
 */
export interface GetBlueprintsResult {
    readonly blueprintIds?: string[];
    /**
     * List of blueprint details.
     */
    readonly blueprintSets: outputs.Lighthouse.GetBlueprintsBlueprintSet[];
    readonly filters?: outputs.Lighthouse.GetBlueprintsFilter[];
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    readonly resultOutputFile?: string;
}
/**
 * Provides a list of Lighthouse blueprints (images).
 *
 * Use this data source to query available blueprints for Lighthouse instances.
 *
 * ## Example Usage
 *
 * ### Query all blueprints:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const all = tencentcloud.lighthouse.getBlueprints({});
 * export const blueprints = all.then(all => all.blueprintSets);
 * ```
 *
 * ### Filter by platform type:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const linux = tencentcloud.lighthouse.getBlueprints({
 *     filters: [{
 *         name: "platform-type",
 *         values: ["LINUX_UNIX"],
 *     }],
 * });
 * ```
 *
 * ### Filter by blueprint type:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const appOs = tencentcloud.lighthouse.getBlueprints({
 *     filters: [{
 *         name: "blueprint-type",
 *         values: ["APP_OS"],
 *     }],
 * });
 * ```
 *
 * ### Query specific blueprints by ID:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const specific = tencentcloud.lighthouse.getBlueprints({
 *     blueprintIds: [
 *         "lhbp-xxx",
 *         "lhbp-yyy",
 *     ],
 * });
 * ```
 */
export function getBlueprintsOutput(args?: GetBlueprintsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetBlueprintsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("tencentcloud:Lighthouse/getBlueprints:getBlueprints", {
        "blueprintIds": args.blueprintIds,
        "filters": args.filters,
        "resultOutputFile": args.resultOutputFile,
    }, opts);
}

/**
 * A collection of arguments for invoking getBlueprints.
 */
export interface GetBlueprintsOutputArgs {
    /**
     * Blueprint ID list.
     */
    blueprintIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Filter list.
     * - `blueprint-id`: Filter by blueprint ID.
     * - `blueprint-type`: Filter by blueprint type. Values: `APP_OS`, `PURE_OS`, `DOCKER`, `PRIVATE`, `SHARED`.
     * - `platform-type`: Filter by platform type. Values: `LINUX_UNIX`, `WINDOWS`.
     * - `blueprint-name`: Filter by blueprint name.
     * - `blueprint-state`: Filter by blueprint state.
     * - `scene-id`: Filter by scene ID.
     *   NOTE: The upper limit of Filters per request is 10. The upper limit of Filter.Values is 100. Parameter does not support specifying both BlueprintIds and Filters.
     */
    filters?: pulumi.Input<pulumi.Input<inputs.Lighthouse.GetBlueprintsFilterArgs>[] | undefined>;
    /**
     * Used to save results.
     */
    resultOutputFile?: pulumi.Input<string | undefined>;
}
