// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a WeData lineage attachment
 *
 * > **NOTE:** Do not use the same relation parameters for lineage binding, as this will cause overwriting.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.wedata.LineageAttachment("example", {relations: {
 *     source: {
 *         resourceUniqueId: "2s5veseIo2AXGOHJkKjBvQ",
 *         resourceType: "TABLE",
 *         platform: "WEDATA",
 *         resourceName: "db_demo.1",
 *         description: "DLC",
 *     },
 *     target: {
 *         resourceUniqueId: "fM8OgzE-AM2h4aaJmdXoPg",
 *         resourceType: "TABLE",
 *         platform: "WEDATA",
 *         resourceName: "db_demo.2",
 *         description: "DLC",
 *     },
 *     processes: {
 *         processId: "20241107221758402",
 *         processType: "SCHEDULE_TASK",
 *         platform: "WEDATA",
 *         processSubType: "SQL_TASK",
 *     },
 * }});
 * ```
 */
export class LineageAttachment extends pulumi.CustomResource {
    /**
     * Get an existing LineageAttachment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: LineageAttachmentState, opts?: pulumi.CustomResourceOptions): LineageAttachment {
        return new LineageAttachment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Wedata/lineageAttachment:LineageAttachment';

    /**
     * Returns true if the given object is an instance of LineageAttachment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is LineageAttachment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === LineageAttachment.__pulumiType;
    }

    /**
     * List of lineage relationships to be registered.
     */
    declare public readonly relations: pulumi.Output<outputs.Wedata.LineageAttachmentRelations>;

    /**
     * Create a LineageAttachment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: LineageAttachmentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: LineageAttachmentArgs | LineageAttachmentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as LineageAttachmentState | undefined;
            resourceInputs["relations"] = state?.relations;
        } else {
            const args = argsOrState as LineageAttachmentArgs | undefined;
            if (args?.relations === undefined && !opts.urn) {
                throw new Error("Missing required property 'relations'");
            }
            resourceInputs["relations"] = args?.relations;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(LineageAttachment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering LineageAttachment resources.
 */
export interface LineageAttachmentState {
    /**
     * List of lineage relationships to be registered.
     */
    relations?: pulumi.Input<inputs.Wedata.LineageAttachmentRelations>;
}

/**
 * The set of arguments for constructing a LineageAttachment resource.
 */
export interface LineageAttachmentArgs {
    /**
     * List of lineage relationships to be registered.
     */
    relations: pulumi.Input<inputs.Wedata.LineageAttachmentRelations>;
}
