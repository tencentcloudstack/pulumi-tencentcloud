// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a dts syncJobResizeOperation
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@pulumi/tencentcloud";
 *
 * const syncJobResizeOperation = new tencentcloud.Dts.SyncJobResizeOperation("sync_job_resize_operation", {
 *     jobId: "sync-werwfs23",
 *     newInstanceClass: "large",
 * });
 * ```
 */
export class SyncJobResizeOperation extends pulumi.CustomResource {
    /**
     * Get an existing SyncJobResizeOperation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SyncJobResizeOperationState, opts?: pulumi.CustomResourceOptions): SyncJobResizeOperation {
        return new SyncJobResizeOperation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Dts/syncJobResizeOperation:SyncJobResizeOperation';

    /**
     * Returns true if the given object is an instance of SyncJobResizeOperation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SyncJobResizeOperation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SyncJobResizeOperation.__pulumiType;
    }

    /**
     * Synchronization instance id (i.e. identifies a synchronization job).
     */
    public readonly jobId!: pulumi.Output<string>;
    /**
     * Task specification.
     */
    public readonly newInstanceClass!: pulumi.Output<string>;

    /**
     * Create a SyncJobResizeOperation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SyncJobResizeOperationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SyncJobResizeOperationArgs | SyncJobResizeOperationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SyncJobResizeOperationState | undefined;
            resourceInputs["jobId"] = state ? state.jobId : undefined;
            resourceInputs["newInstanceClass"] = state ? state.newInstanceClass : undefined;
        } else {
            const args = argsOrState as SyncJobResizeOperationArgs | undefined;
            if ((!args || args.jobId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'jobId'");
            }
            if ((!args || args.newInstanceClass === undefined) && !opts.urn) {
                throw new Error("Missing required property 'newInstanceClass'");
            }
            resourceInputs["jobId"] = args ? args.jobId : undefined;
            resourceInputs["newInstanceClass"] = args ? args.newInstanceClass : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SyncJobResizeOperation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SyncJobResizeOperation resources.
 */
export interface SyncJobResizeOperationState {
    /**
     * Synchronization instance id (i.e. identifies a synchronization job).
     */
    jobId?: pulumi.Input<string>;
    /**
     * Task specification.
     */
    newInstanceClass?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a SyncJobResizeOperation resource.
 */
export interface SyncJobResizeOperationArgs {
    /**
     * Synchronization instance id (i.e. identifies a synchronization job).
     */
    jobId: pulumi.Input<string>;
    /**
     * Task specification.
     */
    newInstanceClass: pulumi.Input<string>;
}
