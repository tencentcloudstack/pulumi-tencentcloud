// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a postgres instance ssl config
 *
 * > **NOTE:** If `sslEnabled` is `false`, Please do not set `connectAddress` field.
 *
 * ## Example Usage
 *
 * ### Enable ssl config
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.postgresql.InstanceSslConfig("example", {
 *     dbInstanceId: "postgres-5wux9sub",
 *     sslEnabled: true,
 *     connectAddress: "10.0.0.12",
 * });
 * ```
 *
 * ### Disable ssl config
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const example = new tencentcloud.postgresql.InstanceSslConfig("example", {
 *     dbInstanceId: "postgres-5wux9sub",
 *     sslEnabled: false,
 * });
 * ```
 *
 * ## Import
 *
 * postgres instance ssl config can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Postgresql/instanceSslConfig:InstanceSslConfig example postgres-5wux9sub
 * ```
 */
export class InstanceSslConfig extends pulumi.CustomResource {
    /**
     * Get an existing InstanceSslConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: InstanceSslConfigState, opts?: pulumi.CustomResourceOptions): InstanceSslConfig {
        return new InstanceSslConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Postgresql/instanceSslConfig:InstanceSslConfig';

    /**
     * Returns true if the given object is an instance of InstanceSslConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is InstanceSslConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === InstanceSslConfig.__pulumiType;
    }

    /**
     * Cloud root certificate download link.
     */
    declare public /*out*/ readonly caUrl: pulumi.Output<string>;
    /**
     * The unique connection address protected by SSL certificate, which can be set as the internal and external IP address if it is the primary instance; If it is a read-only instance, it can be set as the instance IP or read-only group IP. This parameter is mandatory when enabling SSL or modifying SSL protected connection addresses; When SSL is turned off, this parameter will be ignored.
     */
    declare public readonly connectAddress: pulumi.Output<string | undefined>;
    /**
     * Postgres instance ID.
     */
    declare public readonly dbInstanceId: pulumi.Output<string>;
    /**
     * Enable or disable SSL. true: enable; false: disable.
     */
    declare public readonly sslEnabled: pulumi.Output<boolean>;

    /**
     * Create a InstanceSslConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: InstanceSslConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: InstanceSslConfigArgs | InstanceSslConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as InstanceSslConfigState | undefined;
            resourceInputs["caUrl"] = state?.caUrl;
            resourceInputs["connectAddress"] = state?.connectAddress;
            resourceInputs["dbInstanceId"] = state?.dbInstanceId;
            resourceInputs["sslEnabled"] = state?.sslEnabled;
        } else {
            const args = argsOrState as InstanceSslConfigArgs | undefined;
            if (args?.dbInstanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'dbInstanceId'");
            }
            if (args?.sslEnabled === undefined && !opts.urn) {
                throw new Error("Missing required property 'sslEnabled'");
            }
            resourceInputs["connectAddress"] = args?.connectAddress;
            resourceInputs["dbInstanceId"] = args?.dbInstanceId;
            resourceInputs["sslEnabled"] = args?.sslEnabled;
            resourceInputs["caUrl"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(InstanceSslConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering InstanceSslConfig resources.
 */
export interface InstanceSslConfigState {
    /**
     * Cloud root certificate download link.
     */
    caUrl?: pulumi.Input<string>;
    /**
     * The unique connection address protected by SSL certificate, which can be set as the internal and external IP address if it is the primary instance; If it is a read-only instance, it can be set as the instance IP or read-only group IP. This parameter is mandatory when enabling SSL or modifying SSL protected connection addresses; When SSL is turned off, this parameter will be ignored.
     */
    connectAddress?: pulumi.Input<string>;
    /**
     * Postgres instance ID.
     */
    dbInstanceId?: pulumi.Input<string>;
    /**
     * Enable or disable SSL. true: enable; false: disable.
     */
    sslEnabled?: pulumi.Input<boolean>;
}

/**
 * The set of arguments for constructing a InstanceSslConfig resource.
 */
export interface InstanceSslConfigArgs {
    /**
     * The unique connection address protected by SSL certificate, which can be set as the internal and external IP address if it is the primary instance; If it is a read-only instance, it can be set as the instance IP or read-only group IP. This parameter is mandatory when enabling SSL or modifying SSL protected connection addresses; When SSL is turned off, this parameter will be ignored.
     */
    connectAddress?: pulumi.Input<string>;
    /**
     * Postgres instance ID.
     */
    dbInstanceId: pulumi.Input<string>;
    /**
     * Enable or disable SSL. true: enable; false: disable.
     */
    sslEnabled: pulumi.Input<boolean>;
}
