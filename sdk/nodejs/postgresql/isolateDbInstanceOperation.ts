// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a postgresql isolateDbInstanceOperation
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const isolateDbInstanceOperation = new tencentcloud.postgresql.IsolateDbInstanceOperation("isolate_db_instance_operation", {dbInstanceIdSets: [pgsqlId]});
 * ```
 */
export class IsolateDbInstanceOperation extends pulumi.CustomResource {
    /**
     * Get an existing IsolateDbInstanceOperation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: IsolateDbInstanceOperationState, opts?: pulumi.CustomResourceOptions): IsolateDbInstanceOperation {
        return new IsolateDbInstanceOperation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Postgresql/isolateDbInstanceOperation:IsolateDbInstanceOperation';

    /**
     * Returns true if the given object is an instance of IsolateDbInstanceOperation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is IsolateDbInstanceOperation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === IsolateDbInstanceOperation.__pulumiType;
    }

    /**
     * List of resource IDs. Note that currently you cannot isolate multiple instances at the same time. Only one instance ID can be passed in here.
     */
    declare public readonly dbInstanceIdSets: pulumi.Output<string[]>;

    /**
     * Create a IsolateDbInstanceOperation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: IsolateDbInstanceOperationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: IsolateDbInstanceOperationArgs | IsolateDbInstanceOperationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as IsolateDbInstanceOperationState | undefined;
            resourceInputs["dbInstanceIdSets"] = state?.dbInstanceIdSets;
        } else {
            const args = argsOrState as IsolateDbInstanceOperationArgs | undefined;
            if (args?.dbInstanceIdSets === undefined && !opts.urn) {
                throw new Error("Missing required property 'dbInstanceIdSets'");
            }
            resourceInputs["dbInstanceIdSets"] = args?.dbInstanceIdSets;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(IsolateDbInstanceOperation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering IsolateDbInstanceOperation resources.
 */
export interface IsolateDbInstanceOperationState {
    /**
     * List of resource IDs. Note that currently you cannot isolate multiple instances at the same time. Only one instance ID can be passed in here.
     */
    dbInstanceIdSets?: pulumi.Input<pulumi.Input<string>[]>;
}

/**
 * The set of arguments for constructing a IsolateDbInstanceOperation resource.
 */
export interface IsolateDbInstanceOperationArgs {
    /**
     * List of resource IDs. Note that currently you cannot isolate multiple instances at the same time. Only one instance ID can be passed in here.
     */
    dbInstanceIdSets: pulumi.Input<pulumi.Input<string>[]>;
}
