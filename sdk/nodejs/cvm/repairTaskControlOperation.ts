// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to authorize a CVM repair task that is currently in the pending-authorization state, by invoking the `RepairTaskControl` cloud API.
 *
 * This is an operation-style resource: only `Create` triggers the cloud-side action. `Read` and `Delete` are no-ops because the underlying API has no `Describe` or reverse counterpart. Any change to the input arguments forces the resource to be re-created (all fields are `ForceNew`). To re-trigger an authorization, use `terraform taint`.
 *
 * Use the `tencentcloud.Cvm.getRepairTasks` data source to discover repair tasks (e.g., those with `taskStatus = 1`, pending authorization) before authorizing them with this resource.
 *
 * ## Example Usage
 *
 * ### Authorize a repair task immediately
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const demo = new tencentcloud.cvm.RepairTaskControlOperation("demo", {
 *     product: "CVM",
 *     instanceIds: ["ins-xxxxxxxx"],
 *     taskId: "rep-xxxxxxxx",
 *     operate: "AuthorizeRepair",
 * });
 * ```
 *
 * ### Schedule the authorization at a future time
 *
 * The scheduled time must be at least 5 minutes later than the current time and within 48 hours.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const scheduled = new tencentcloud.cvm.RepairTaskControlOperation("scheduled", {
 *     product: "CVM",
 *     instanceIds: ["ins-xxxxxxxx"],
 *     taskId: "rep-xxxxxxxx",
 *     operate: "AuthorizeRepair",
 *     orderAuthTime: "2030-01-01 12:00:00",
 * });
 * ```
 *
 * ### Authorize with lossy local-disk migration
 *
 * WARNING: setting taskSubMethod to LossyLocal on a local-disk instance will WIPE ALL LOCAL DISK DATA, equivalent to redeploying the local-disk instance. Make sure to back up critical data and review your /etc/fstab mounts (consider adding nofail) before proceeding.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const lossyLocal = new tencentcloud.cvm.RepairTaskControlOperation("lossy_local", {
 *     product: "CVM",
 *     instanceIds: ["ins-xxxxxxxx"],
 *     taskId: "rep-xxxxxxxx",
 *     operate: "AuthorizeRepair",
 *     taskSubMethod: "LossyLocal",
 * });
 * ```
 *
 * ## Import
 *
 * This resource is an operation-style resource and does not support `pulumi import`. The cloud-side authorization is a one-shot side-effect with no `Describe` API to refresh state.
 */
export class RepairTaskControlOperation extends pulumi.CustomResource {
    /**
     * Get an existing RepairTaskControlOperation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RepairTaskControlOperationState, opts?: pulumi.CustomResourceOptions): RepairTaskControlOperation {
        return new RepairTaskControlOperation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Cvm/repairTaskControlOperation:RepairTaskControlOperation';

    /**
     * Returns true if the given object is an instance of RepairTaskControlOperation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RepairTaskControlOperation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RepairTaskControlOperation.__pulumiType;
    }

    /**
     * List of instance IDs to operate on. Only repair tasks related to these instance IDs are authorized. Can be obtained from `InstanceId` in the `DescribeTaskInfo` API response.
     */
    declare public readonly instanceIds: pulumi.Output<string[]>;
    /**
     * Operation type. Currently only `AuthorizeRepair` is supported.
     */
    declare public readonly operate: pulumi.Output<string>;
    /**
     * Scheduled authorization time, format `YYYY-MM-DD HH:MM:SS`. The scheduled time must be at least 5 minutes later than the current time and within 48 hours.
     */
    declare public readonly orderAuthTime: pulumi.Output<string | undefined>;
    /**
     * Product type the pending-authorization task instance belongs to. Valid values: `CVM` (Cloud Virtual Machine), `CDH` (Cloud Dedicated Host), `CPM2.0` (Cloud Physical Machine 2.0).
     */
    declare public readonly product: pulumi.Output<string>;
    /**
     * ID of the repair task to operate on. Can be obtained from `TaskId` in the `DescribeTaskInfo` API response.
     */
    declare public readonly taskId: pulumi.Output<string>;
    /**
     * Additional authorization handling strategy. When empty, the default authorization is used. For repair tasks supporting lossy migration, set to `LossyLocal` to allow lossy local-disk migration. WARNING: when `LossyLocal` is used on a local-disk instance, all local disk data will be wiped, equivalent to redeploying the local-disk instance.
     */
    declare public readonly taskSubMethod: pulumi.Output<string | undefined>;

    /**
     * Create a RepairTaskControlOperation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RepairTaskControlOperationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RepairTaskControlOperationArgs | RepairTaskControlOperationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RepairTaskControlOperationState | undefined;
            resourceInputs["instanceIds"] = state?.instanceIds;
            resourceInputs["operate"] = state?.operate;
            resourceInputs["orderAuthTime"] = state?.orderAuthTime;
            resourceInputs["product"] = state?.product;
            resourceInputs["taskId"] = state?.taskId;
            resourceInputs["taskSubMethod"] = state?.taskSubMethod;
        } else {
            const args = argsOrState as RepairTaskControlOperationArgs | undefined;
            if (args?.instanceIds === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceIds'");
            }
            if (args?.operate === undefined && !opts.urn) {
                throw new Error("Missing required property 'operate'");
            }
            if (args?.product === undefined && !opts.urn) {
                throw new Error("Missing required property 'product'");
            }
            if (args?.taskId === undefined && !opts.urn) {
                throw new Error("Missing required property 'taskId'");
            }
            resourceInputs["instanceIds"] = args?.instanceIds;
            resourceInputs["operate"] = args?.operate;
            resourceInputs["orderAuthTime"] = args?.orderAuthTime;
            resourceInputs["product"] = args?.product;
            resourceInputs["taskId"] = args?.taskId;
            resourceInputs["taskSubMethod"] = args?.taskSubMethod;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RepairTaskControlOperation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RepairTaskControlOperation resources.
 */
export interface RepairTaskControlOperationState {
    /**
     * List of instance IDs to operate on. Only repair tasks related to these instance IDs are authorized. Can be obtained from `InstanceId` in the `DescribeTaskInfo` API response.
     */
    instanceIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Operation type. Currently only `AuthorizeRepair` is supported.
     */
    operate?: pulumi.Input<string | undefined>;
    /**
     * Scheduled authorization time, format `YYYY-MM-DD HH:MM:SS`. The scheduled time must be at least 5 minutes later than the current time and within 48 hours.
     */
    orderAuthTime?: pulumi.Input<string | undefined>;
    /**
     * Product type the pending-authorization task instance belongs to. Valid values: `CVM` (Cloud Virtual Machine), `CDH` (Cloud Dedicated Host), `CPM2.0` (Cloud Physical Machine 2.0).
     */
    product?: pulumi.Input<string | undefined>;
    /**
     * ID of the repair task to operate on. Can be obtained from `TaskId` in the `DescribeTaskInfo` API response.
     */
    taskId?: pulumi.Input<string | undefined>;
    /**
     * Additional authorization handling strategy. When empty, the default authorization is used. For repair tasks supporting lossy migration, set to `LossyLocal` to allow lossy local-disk migration. WARNING: when `LossyLocal` is used on a local-disk instance, all local disk data will be wiped, equivalent to redeploying the local-disk instance.
     */
    taskSubMethod?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a RepairTaskControlOperation resource.
 */
export interface RepairTaskControlOperationArgs {
    /**
     * List of instance IDs to operate on. Only repair tasks related to these instance IDs are authorized. Can be obtained from `InstanceId` in the `DescribeTaskInfo` API response.
     */
    instanceIds: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Operation type. Currently only `AuthorizeRepair` is supported.
     */
    operate: pulumi.Input<string>;
    /**
     * Scheduled authorization time, format `YYYY-MM-DD HH:MM:SS`. The scheduled time must be at least 5 minutes later than the current time and within 48 hours.
     */
    orderAuthTime?: pulumi.Input<string | undefined>;
    /**
     * Product type the pending-authorization task instance belongs to. Valid values: `CVM` (Cloud Virtual Machine), `CDH` (Cloud Dedicated Host), `CPM2.0` (Cloud Physical Machine 2.0).
     */
    product: pulumi.Input<string>;
    /**
     * ID of the repair task to operate on. Can be obtained from `TaskId` in the `DescribeTaskInfo` API response.
     */
    taskId: pulumi.Input<string>;
    /**
     * Additional authorization handling strategy. When empty, the default authorization is used. For repair tasks supporting lossy migration, set to `LossyLocal` to allow lossy local-disk migration. WARNING: when `LossyLocal` is used on a local-disk instance, all local disk data will be wiped, equivalent to redeploying the local-disk instance.
     */
    taskSubMethod?: pulumi.Input<string | undefined>;
}
