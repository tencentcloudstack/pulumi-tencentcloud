// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

export class RenewHost extends pulumi.CustomResource {
    /**
     * Get an existing RenewHost resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RenewHostState, opts?: pulumi.CustomResourceOptions): RenewHost {
        return new RenewHost(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Cvm/renewHost:RenewHost';

    /**
     * Returns true if the given object is an instance of RenewHost.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RenewHost {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RenewHost.__pulumiType;
    }

    /**
     * Prepaid mode, that is, yearly and monthly subscription related parameter settings. Through this parameter, you can
     * specify attributes such as the purchase duration of the Subscription instance and whether to set automatic renewal. If
     * the payment mode of the specified instance is prepaid, this parameter must be passed.
     */
    public readonly hostChargePrepaid!: pulumi.Output<outputs.Cvm.RenewHostHostChargePrepaid>;
    /**
     * CDH instance ID.
     */
    public readonly hostId!: pulumi.Output<string>;

    /**
     * Create a RenewHost resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RenewHostArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RenewHostArgs | RenewHostState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RenewHostState | undefined;
            resourceInputs["hostChargePrepaid"] = state ? state.hostChargePrepaid : undefined;
            resourceInputs["hostId"] = state ? state.hostId : undefined;
        } else {
            const args = argsOrState as RenewHostArgs | undefined;
            if ((!args || args.hostChargePrepaid === undefined) && !opts.urn) {
                throw new Error("Missing required property 'hostChargePrepaid'");
            }
            if ((!args || args.hostId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'hostId'");
            }
            resourceInputs["hostChargePrepaid"] = args ? args.hostChargePrepaid : undefined;
            resourceInputs["hostId"] = args ? args.hostId : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RenewHost.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RenewHost resources.
 */
export interface RenewHostState {
    /**
     * Prepaid mode, that is, yearly and monthly subscription related parameter settings. Through this parameter, you can
     * specify attributes such as the purchase duration of the Subscription instance and whether to set automatic renewal. If
     * the payment mode of the specified instance is prepaid, this parameter must be passed.
     */
    hostChargePrepaid?: pulumi.Input<inputs.Cvm.RenewHostHostChargePrepaid>;
    /**
     * CDH instance ID.
     */
    hostId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a RenewHost resource.
 */
export interface RenewHostArgs {
    /**
     * Prepaid mode, that is, yearly and monthly subscription related parameter settings. Through this parameter, you can
     * specify attributes such as the purchase duration of the Subscription instance and whether to set automatic renewal. If
     * the payment mode of the specified instance is prepaid, this parameter must be passed.
     */
    hostChargePrepaid: pulumi.Input<inputs.Cvm.RenewHostHostChargePrepaid>;
    /**
     * CDH instance ID.
     */
    hostId: pulumi.Input<string>;
}
