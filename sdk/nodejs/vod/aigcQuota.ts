// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to manage a VOD AIGC Quota for a specific sub application.
 *
 * ## Example Usage
 *
 * ### If quota type is Image
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * // create sub application
 * const example = new tencentcloud.vod.SubApplication("example", {
 *     name: "tf-example",
 *     status: "On",
 *     description: "this is sub application",
 * });
 * const exampleAigcQuota = new tencentcloud.vod.AigcQuota("example", {
 *     subAppId: example.subAppId.apply(x =>Number(x)),
 *     quotaType: "Image",
 *     quotaLimit: 100,
 * });
 * ```
 *
 * ### If quota type is Video
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * // create sub application
 * const example = new tencentcloud.vod.SubApplication("example", {
 *     name: "tf-example",
 *     status: "On",
 *     description: "this is sub application",
 * });
 * const exampleAigcQuota = new tencentcloud.vod.AigcQuota("example", {
 *     subAppId: example.subAppId.apply(x =>Number(x)),
 *     quotaType: "Video",
 *     quotaLimit: 200,
 * });
 * ```
 *
 * ### If quota type is Text
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * // create sub application
 * const example = new tencentcloud.vod.SubApplication("example", {
 *     name: "tf-example",
 *     status: "On",
 *     description: "this is sub application",
 * });
 * // create api token
 * const exampleAigcApiToken = new tencentcloud.vod.AigcApiToken("example", {subAppId: example.subAppId.apply(x =>Number(x))});
 * const exampleAigcQuota = new tencentcloud.vod.AigcQuota("example", {
 *     subAppId: example.subAppId.apply(x =>Number(x)),
 *     quotaType: "Text",
 *     quotaLimit: 50,
 *     apiToken: exampleAigcApiToken.apiToken,
 * });
 * ```
 *
 * ## Import
 *
 * VOD AIGC Quota can be imported using the composite id `sub_app_id#quota_type#api_token`, e.g.
 *
 * If quota type is Image/Video
 *
 * ```sh
 * $ pulumi import tencentcloud:Vod/aigcQuota:AigcQuota example 1500066373#Image
 * $ pulumi import tencentcloud:Vod/aigcQuota:AigcQuota example 1500066373#Video
 * ```
 *
 * If quota type is Text
 *
 * ```sh
 * terraform import tencentcloud_vod_aigc_quota.example 1500066373#Text#<YOUR TOKEN>
 * ```
 */
export class AigcQuota extends pulumi.CustomResource {
    /**
     * Get an existing AigcQuota resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AigcQuotaState, opts?: pulumi.CustomResourceOptions): AigcQuota {
        return new AigcQuota(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Vod/aigcQuota:AigcQuota';

    /**
     * Returns true if the given object is an instance of AigcQuota.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AigcQuota {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AigcQuota.__pulumiType;
    }

    /**
     * API token for quota restriction. Only meaningful when `quotaType` is `Text`.
     */
    declare public readonly apiToken: pulumi.Output<string | undefined>;
    /**
     * Quota limit value. Unit: when `quotaType` is `Image`, count by images; when `quotaType` is `Video`, count by seconds; when `quotaType` is `Text`, count by tokens.
     */
    declare public readonly quotaLimit: pulumi.Output<number>;
    /**
     * Quota type. Valid values: `Image` (AIGC image generation), `Video` (AIGC video generation), `Text` (AIGC text generation).
     */
    declare public readonly quotaType: pulumi.Output<string>;
    /**
     * The VOD [sub application](https://intl.cloud.tencent.com/document/product/266/14574) ID. Users who activated VOD service after December 25, 2023 must specify the application ID.
     */
    declare public readonly subAppId: pulumi.Output<number>;
    /**
     * Current usage amount. Unit: when `quotaType` is `Image`, count by images; when `quotaType` is `Video`, count by seconds; when `quotaType` is `Text`, count by tokens.
     */
    declare public /*out*/ readonly usage: pulumi.Output<number>;

    /**
     * Create a AigcQuota resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AigcQuotaArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AigcQuotaArgs | AigcQuotaState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AigcQuotaState | undefined;
            resourceInputs["apiToken"] = state?.apiToken;
            resourceInputs["quotaLimit"] = state?.quotaLimit;
            resourceInputs["quotaType"] = state?.quotaType;
            resourceInputs["subAppId"] = state?.subAppId;
            resourceInputs["usage"] = state?.usage;
        } else {
            const args = argsOrState as AigcQuotaArgs | undefined;
            if (args?.quotaLimit === undefined && !opts.urn) {
                throw new Error("Missing required property 'quotaLimit'");
            }
            if (args?.quotaType === undefined && !opts.urn) {
                throw new Error("Missing required property 'quotaType'");
            }
            if (args?.subAppId === undefined && !opts.urn) {
                throw new Error("Missing required property 'subAppId'");
            }
            resourceInputs["apiToken"] = args?.apiToken;
            resourceInputs["quotaLimit"] = args?.quotaLimit;
            resourceInputs["quotaType"] = args?.quotaType;
            resourceInputs["subAppId"] = args?.subAppId;
            resourceInputs["usage"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AigcQuota.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AigcQuota resources.
 */
export interface AigcQuotaState {
    /**
     * API token for quota restriction. Only meaningful when `quotaType` is `Text`.
     */
    apiToken?: pulumi.Input<string | undefined>;
    /**
     * Quota limit value. Unit: when `quotaType` is `Image`, count by images; when `quotaType` is `Video`, count by seconds; when `quotaType` is `Text`, count by tokens.
     */
    quotaLimit?: pulumi.Input<number | undefined>;
    /**
     * Quota type. Valid values: `Image` (AIGC image generation), `Video` (AIGC video generation), `Text` (AIGC text generation).
     */
    quotaType?: pulumi.Input<string | undefined>;
    /**
     * The VOD [sub application](https://intl.cloud.tencent.com/document/product/266/14574) ID. Users who activated VOD service after December 25, 2023 must specify the application ID.
     */
    subAppId?: pulumi.Input<number | undefined>;
    /**
     * Current usage amount. Unit: when `quotaType` is `Image`, count by images; when `quotaType` is `Video`, count by seconds; when `quotaType` is `Text`, count by tokens.
     */
    usage?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a AigcQuota resource.
 */
export interface AigcQuotaArgs {
    /**
     * API token for quota restriction. Only meaningful when `quotaType` is `Text`.
     */
    apiToken?: pulumi.Input<string | undefined>;
    /**
     * Quota limit value. Unit: when `quotaType` is `Image`, count by images; when `quotaType` is `Video`, count by seconds; when `quotaType` is `Text`, count by tokens.
     */
    quotaLimit: pulumi.Input<number>;
    /**
     * Quota type. Valid values: `Image` (AIGC image generation), `Video` (AIGC video generation), `Text` (AIGC text generation).
     */
    quotaType: pulumi.Input<string>;
    /**
     * The VOD [sub application](https://intl.cloud.tencent.com/document/product/266/14574) ID. Users who activated VOD service after December 25, 2023 must specify the application ID.
     */
    subAppId: pulumi.Input<number>;
}
