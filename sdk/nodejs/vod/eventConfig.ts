// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provide a resource to create a vod event config.
 *
 * ## Import
 *
 * VOD event config can be imported using the subAppId, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Vod/eventConfig:EventConfig foo $subAppId
 * ```
 */
export class EventConfig extends pulumi.CustomResource {
    /**
     * Get an existing EventConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: EventConfigState, opts?: pulumi.CustomResourceOptions): EventConfig {
        return new EventConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Vod/eventConfig:EventConfig';

    /**
     * Returns true if the given object is an instance of EventConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is EventConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === EventConfig.__pulumiType;
    }

    /**
     * Whether to receive video deletion completion event notification, default `OFF` is to ignore the event notification, `ON` is to receive event notification.
     */
    public readonly deleteMediaCompleteEventSwitch!: pulumi.Output<string>;
    /**
     * How to receive event notifications. Valid values:
     * - Push: HTTP callback notification;
     * - PULL: Reliable notification based on message queuing.
     */
    public readonly mode!: pulumi.Output<string>;
    /**
     * The address used to receive 3.0 format callbacks when receiving HTTP callback notifications. Note: If you take the NotificationUrl parameter and the value is an empty string, the 3.0 format callback address is cleared.
     */
    public readonly notificationUrl!: pulumi.Output<string | undefined>;
    /**
     * Sub app id.
     */
    public readonly subAppId!: pulumi.Output<number>;
    /**
     * Whether to receive video upload completion event notification, default `OFF` means to ignore the event notification, `ON` means to receive event notification.
     */
    public readonly uploadMediaCompleteEventSwitch!: pulumi.Output<string>;

    /**
     * Create a EventConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: EventConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: EventConfigArgs | EventConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as EventConfigState | undefined;
            resourceInputs["deleteMediaCompleteEventSwitch"] = state ? state.deleteMediaCompleteEventSwitch : undefined;
            resourceInputs["mode"] = state ? state.mode : undefined;
            resourceInputs["notificationUrl"] = state ? state.notificationUrl : undefined;
            resourceInputs["subAppId"] = state ? state.subAppId : undefined;
            resourceInputs["uploadMediaCompleteEventSwitch"] = state ? state.uploadMediaCompleteEventSwitch : undefined;
        } else {
            const args = argsOrState as EventConfigArgs | undefined;
            if ((!args || args.subAppId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'subAppId'");
            }
            resourceInputs["deleteMediaCompleteEventSwitch"] = args ? args.deleteMediaCompleteEventSwitch : undefined;
            resourceInputs["mode"] = args ? args.mode : undefined;
            resourceInputs["notificationUrl"] = args ? args.notificationUrl : undefined;
            resourceInputs["subAppId"] = args ? args.subAppId : undefined;
            resourceInputs["uploadMediaCompleteEventSwitch"] = args ? args.uploadMediaCompleteEventSwitch : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(EventConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering EventConfig resources.
 */
export interface EventConfigState {
    /**
     * Whether to receive video deletion completion event notification, default `OFF` is to ignore the event notification, `ON` is to receive event notification.
     */
    deleteMediaCompleteEventSwitch?: pulumi.Input<string>;
    /**
     * How to receive event notifications. Valid values:
     * - Push: HTTP callback notification;
     * - PULL: Reliable notification based on message queuing.
     */
    mode?: pulumi.Input<string>;
    /**
     * The address used to receive 3.0 format callbacks when receiving HTTP callback notifications. Note: If you take the NotificationUrl parameter and the value is an empty string, the 3.0 format callback address is cleared.
     */
    notificationUrl?: pulumi.Input<string>;
    /**
     * Sub app id.
     */
    subAppId?: pulumi.Input<number>;
    /**
     * Whether to receive video upload completion event notification, default `OFF` means to ignore the event notification, `ON` means to receive event notification.
     */
    uploadMediaCompleteEventSwitch?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a EventConfig resource.
 */
export interface EventConfigArgs {
    /**
     * Whether to receive video deletion completion event notification, default `OFF` is to ignore the event notification, `ON` is to receive event notification.
     */
    deleteMediaCompleteEventSwitch?: pulumi.Input<string>;
    /**
     * How to receive event notifications. Valid values:
     * - Push: HTTP callback notification;
     * - PULL: Reliable notification based on message queuing.
     */
    mode?: pulumi.Input<string>;
    /**
     * The address used to receive 3.0 format callbacks when receiving HTTP callback notifications. Note: If you take the NotificationUrl parameter and the value is an empty string, the 3.0 format callback address is cleared.
     */
    notificationUrl?: pulumi.Input<string>;
    /**
     * Sub app id.
     */
    subAppId: pulumi.Input<number>;
    /**
     * Whether to receive video upload completion event notification, default `OFF` means to ignore the event notification, `ON` means to receive event notification.
     */
    uploadMediaCompleteEventSwitch?: pulumi.Input<string>;
}
