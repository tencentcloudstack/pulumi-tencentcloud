// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Provides a resource to manage a VOD AIGC API Token for a specific sub application.
 *
 * An AIGC API Token is an independent credential used when calling VOD AIGC-related APIs. One sub application can hold multiple tokens. The token value is returned by the cloud on creation and is treated as a sensitive value.
 *
 * > **NOTE:** The cloud side has an approximately 30-second data-sync delay before a newly created or deleted token becomes visible via `DescribeAigcApiTokens`. The provider polls the list internally, so `pulumi up` may take up to a few minutes.
 *
 * > **NOTE:** `apiToken` is marked `Sensitive` and is still stored in state in plain text. Protect your Terraform state accordingly (remote state encryption, restricted access, etc.).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * // create sub application
 * const example = new tencentcloud.vod.SubApplication("example", {
 *     name: "tf-example",
 *     status: "On",
 *     description: "this is sub application",
 * });
 * // create api token
 * const exampleAigcApiToken = new tencentcloud.vod.AigcApiToken("example", {subAppId: example.subAppId.apply(x =>Number(x))});
 * export const aigcApiToken = exampleAigcApiToken.apiToken;
 * ```
 *
 * ## Import
 *
 * VOD AIGC API Token can be imported using the composite id `sub_app_id#api_token`, e.g.
 *
 * ```sh
 * terraform import tencentcloud_vod_aigc_api_token.example 1500066380#<YOUR TOKEN>
 * ```
 */
export class AigcApiToken extends pulumi.CustomResource {
    /**
     * Get an existing AigcApiToken resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AigcApiTokenState, opts?: pulumi.CustomResourceOptions): AigcApiToken {
        return new AigcApiToken(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Vod/aigcApiToken:AigcApiToken';

    /**
     * Returns true if the given object is an instance of AigcApiToken.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AigcApiToken {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AigcApiToken.__pulumiType;
    }

    /**
     * The AIGC API Token value generated by the cloud side. Sensitive value; stored in state in plain text. Optional during `pulumi import` to accept a pre-existing token, but SHOULD NOT be set in regular `.tf` configurations.
     */
    declare public /*out*/ readonly apiToken: pulumi.Output<string>;
    /**
     * The VOD [sub application](https://intl.cloud.tencent.com/document/product/266/14574) ID. Users who activated VOD service after December 25, 2023 must specify the application ID.
     */
    declare public readonly subAppId: pulumi.Output<number>;

    /**
     * Create a AigcApiToken resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AigcApiTokenArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AigcApiTokenArgs | AigcApiTokenState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AigcApiTokenState | undefined;
            resourceInputs["apiToken"] = state?.apiToken;
            resourceInputs["subAppId"] = state?.subAppId;
        } else {
            const args = argsOrState as AigcApiTokenArgs | undefined;
            if (args?.subAppId === undefined && !opts.urn) {
                throw new Error("Missing required property 'subAppId'");
            }
            resourceInputs["subAppId"] = args?.subAppId;
            resourceInputs["apiToken"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["apiToken"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(AigcApiToken.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AigcApiToken resources.
 */
export interface AigcApiTokenState {
    /**
     * The AIGC API Token value generated by the cloud side. Sensitive value; stored in state in plain text. Optional during `pulumi import` to accept a pre-existing token, but SHOULD NOT be set in regular `.tf` configurations.
     */
    apiToken?: pulumi.Input<string | undefined>;
    /**
     * The VOD [sub application](https://intl.cloud.tencent.com/document/product/266/14574) ID. Users who activated VOD service after December 25, 2023 must specify the application ID.
     */
    subAppId?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a AigcApiToken resource.
 */
export interface AigcApiTokenArgs {
    /**
     * The VOD [sub application](https://intl.cloud.tencent.com/document/product/266/14574) ID. Users who activated VOD service after December 25, 2023 must specify the application ID.
     */
    subAppId: pulumi.Input<number>;
}
