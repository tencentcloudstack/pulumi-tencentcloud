// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Use this data source to query detailed information of VOD sub-applications.
 *
 * ## Example Usage
 *
 * ### Query all sub-applications
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const all = tencentcloud.vod.getSubApplications({});
 * export const appList = all.then(all => all.subApplicationInfoSets);
 * ```
 *
 * ### Filter by application name
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const byName = tencentcloud.vod.getSubApplications({
 *     name: "MyVideoApp",
 * });
 * export const appId = byName.then(byName => byName.subApplicationInfoSets?.[0]?.subAppId);
 * ```
 *
 * ### Filter by tags
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const byTags = tencentcloud.vod.getSubApplications({
 *     tags: {
 *         Environment: "Production",
 *         Team: "VideoTeam",
 *     },
 * });
 * export const productionApps = byTags.then(byTags => byTags.subApplicationInfoSets.map(__item => __item.subAppIdName));
 * ```
 *
 * ### Reference sub-application in other resources
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const existing = tencentcloud.vod.getSubApplications({
 *     name: "ProductionApp",
 * });
 * const config = new tencentcloud.vod.SuperPlayerConfig("config", {
 *     subAppId: existing.then(existing => existing.subApplicationInfoSets?.[0]?.subAppId),
 *     name: "player-config",
 *     drmSwitch: false,
 * });
 * ```
 *
 * ### Export results to file
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const _export = tencentcloud.vod.getSubApplications({
 *     resultOutputFile: "/tmp/vod_apps.json",
 * });
 * ```
 */
export function getSubApplications(args?: GetSubApplicationsArgs, opts?: pulumi.InvokeOptions): Promise<GetSubApplicationsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("tencentcloud:Vod/getSubApplications:getSubApplications", {
        "name": args.name,
        "resultOutputFile": args.resultOutputFile,
        "tags": args.tags,
    }, opts);
}

/**
 * A collection of arguments for invoking getSubApplications.
 */
export interface GetSubApplicationsArgs {
    /**
     * Application name for exact match filtering.
     */
    name?: string;
    /**
     * Used to save results in JSON format.
     */
    resultOutputFile?: string;
    /**
     * Tag key-value pairs for filtering applications. Applications matching all specified tags will be returned.
     */
    tags?: {[key: string]: string};
}

/**
 * A collection of values returned by getSubApplications.
 */
export interface GetSubApplicationsResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * Legacy name field (for backward compatibility).
     */
    readonly name?: string;
    readonly resultOutputFile?: string;
    /**
     * List of sub-application information.
     */
    readonly subApplicationInfoSets: outputs.Vod.GetSubApplicationsSubApplicationInfoSet[];
    /**
     * Resource tags bound to the sub-application.
     */
    readonly tags?: {[key: string]: string};
}
/**
 * Use this data source to query detailed information of VOD sub-applications.
 *
 * ## Example Usage
 *
 * ### Query all sub-applications
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const all = tencentcloud.vod.getSubApplications({});
 * export const appList = all.then(all => all.subApplicationInfoSets);
 * ```
 *
 * ### Filter by application name
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const byName = tencentcloud.vod.getSubApplications({
 *     name: "MyVideoApp",
 * });
 * export const appId = byName.then(byName => byName.subApplicationInfoSets?.[0]?.subAppId);
 * ```
 *
 * ### Filter by tags
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const byTags = tencentcloud.vod.getSubApplications({
 *     tags: {
 *         Environment: "Production",
 *         Team: "VideoTeam",
 *     },
 * });
 * export const productionApps = byTags.then(byTags => byTags.subApplicationInfoSets.map(__item => __item.subAppIdName));
 * ```
 *
 * ### Reference sub-application in other resources
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const existing = tencentcloud.vod.getSubApplications({
 *     name: "ProductionApp",
 * });
 * const config = new tencentcloud.vod.SuperPlayerConfig("config", {
 *     subAppId: existing.then(existing => existing.subApplicationInfoSets?.[0]?.subAppId),
 *     name: "player-config",
 *     drmSwitch: false,
 * });
 * ```
 *
 * ### Export results to file
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const _export = tencentcloud.vod.getSubApplications({
 *     resultOutputFile: "/tmp/vod_apps.json",
 * });
 * ```
 */
export function getSubApplicationsOutput(args?: GetSubApplicationsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetSubApplicationsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("tencentcloud:Vod/getSubApplications:getSubApplications", {
        "name": args.name,
        "resultOutputFile": args.resultOutputFile,
        "tags": args.tags,
    }, opts);
}

/**
 * A collection of arguments for invoking getSubApplications.
 */
export interface GetSubApplicationsOutputArgs {
    /**
     * Application name for exact match filtering.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Used to save results in JSON format.
     */
    resultOutputFile?: pulumi.Input<string | undefined>;
    /**
     * Tag key-value pairs for filtering applications. Applications matching all specified tags will be returned.
     */
    tags?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
}
