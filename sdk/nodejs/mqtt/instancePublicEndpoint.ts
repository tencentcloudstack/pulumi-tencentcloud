// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Provides a resource to create a MQTT instance public endpoint
 *
 * > **NOTE:** This resource must exclusive in one MQTT instance, do not declare additional public endpoint resources of this instance elsewhere.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as tencentcloud from "@tencentcloud_iac/pulumi";
 *
 * const config = new pulumi.Config();
 * const availabilityZone = config.get("availabilityZone") || "ap-guangzhou-6";
 * // create vpc
 * const vpc = new tencentcloud.vpc.Instance("vpc", {
 *     cidrBlock: "10.0.0.0/16",
 *     name: "vpc",
 * });
 * // create subnet
 * const subnet = new tencentcloud.subnet.Instance("subnet", {
 *     vpcId: vpc.id,
 *     availabilityZone: availabilityZone,
 *     name: "subnet",
 *     cidrBlock: "10.0.1.0/24",
 *     isMulticast: false,
 * });
 * // create mqtt instance
 * const example = new tencentcloud.mqtt.Instance("example", {
 *     instanceType: "BASIC",
 *     name: "tf-example",
 *     skuCode: "basic_2k",
 *     remark: "remarks.",
 *     vpcList: {
 *         vpcId: vpc.id,
 *         subnetId: subnet.id,
 *     },
 *     payMode: 0,
 *     tags: {
 *         createBy: "Terraform",
 *     },
 * });
 * // create public endpoint
 * const exampleInstancePublicEndpoint = new tencentcloud.mqtt.InstancePublicEndpoint("example", {
 *     instanceId: example.id,
 *     bandwidth: 100,
 *     rules: [
 *         {
 *             ipRule: "192.168.1.0/24",
 *             remark: "Remark.",
 *         },
 *         {
 *             ipRule: "172.16.1.0/24",
 *             remark: "Remark.",
 *         },
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * MQTT instance public endpoint can be imported using the id, e.g.
 *
 * ```sh
 * $ pulumi import tencentcloud:Mqtt/instancePublicEndpoint:InstancePublicEndpoint example mqtt-emb2v5wk
 * ```
 */
export class InstancePublicEndpoint extends pulumi.CustomResource {
    /**
     * Get an existing InstancePublicEndpoint resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: InstancePublicEndpointState, opts?: pulumi.CustomResourceOptions): InstancePublicEndpoint {
        return new InstancePublicEndpoint(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tencentcloud:Mqtt/instancePublicEndpoint:InstancePublicEndpoint';

    /**
     * Returns true if the given object is an instance of InstancePublicEndpoint.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is InstancePublicEndpoint {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === InstancePublicEndpoint.__pulumiType;
    }

    /**
     * Bandwidth, unit: Mbps.
     */
    declare public readonly bandwidth: pulumi.Output<number>;
    /**
     * MQTT instance ID.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * White List of Public Network Access Rules.
     */
    declare public readonly rules: pulumi.Output<outputs.Mqtt.InstancePublicEndpointRule[] | undefined>;

    /**
     * Create a InstancePublicEndpoint resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: InstancePublicEndpointArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: InstancePublicEndpointArgs | InstancePublicEndpointState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as InstancePublicEndpointState | undefined;
            resourceInputs["bandwidth"] = state?.bandwidth;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["rules"] = state?.rules;
        } else {
            const args = argsOrState as InstancePublicEndpointArgs | undefined;
            if (args?.bandwidth === undefined && !opts.urn) {
                throw new Error("Missing required property 'bandwidth'");
            }
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            resourceInputs["bandwidth"] = args?.bandwidth;
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["rules"] = args?.rules;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(InstancePublicEndpoint.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering InstancePublicEndpoint resources.
 */
export interface InstancePublicEndpointState {
    /**
     * Bandwidth, unit: Mbps.
     */
    bandwidth?: pulumi.Input<number>;
    /**
     * MQTT instance ID.
     */
    instanceId?: pulumi.Input<string>;
    /**
     * White List of Public Network Access Rules.
     */
    rules?: pulumi.Input<pulumi.Input<inputs.Mqtt.InstancePublicEndpointRule>[]>;
}

/**
 * The set of arguments for constructing a InstancePublicEndpoint resource.
 */
export interface InstancePublicEndpointArgs {
    /**
     * Bandwidth, unit: Mbps.
     */
    bandwidth: pulumi.Input<number>;
    /**
     * MQTT instance ID.
     */
    instanceId: pulumi.Input<string>;
    /**
     * White List of Public Network Access Rules.
     */
    rules?: pulumi.Input<pulumi.Input<inputs.Mqtt.InstancePublicEndpointRule>[]>;
}
